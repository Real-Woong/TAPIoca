# 전략 현황

> **지금 무엇이 참인가**만 적는다. 취소선도, 진행 표시도, "예전에는" 도 없다.
> 사실이 바뀌면 이 문서를 고치고, 무슨 일이 있었는지는 `develope-log/`에 남긴다.
>
> 마지막 갱신: 2026-08-07

---

## 한 줄로

**이 시스템은 시장보다 더 버는 장치가 아니라 덜 잃는 장치다.**
연 1.9~2.6%p의 수익을 최대 낙폭 12~23%p와 바꾼다. 의도된 거래다.

---

## 1. 확정된 것

### ① 신호 스택은 정적 배분을 낙폭에서 이긴다

같은 주식 비중으로 맞춰 비교했을 때, 20년 실데이터 기준:

| 표본 | 우리 낙폭 | 같은 노출의 정적 배분 | 우위 |
|---|---|---|---|
| 2006~2026 (2008 포함) | 29.8% | 45.8% | **15.9%p** |
| 2011~2026 | 18.7% | 29.1% | **10.3%p** |

**수익률 우위는 없다.** Sharpe 차이는 구간별 부호가 갈린다(3/4 ✗). 주장할 수 없다.
**사는 것은 오직 낙폭이다.**

> **단, 낙폭 우위도 4/4는 아니다.** 정적 70/30 대비 MDD 차이가 구간별로
> +15.0 / **−1.6** / +8.0 / +0.5 (**3/4 ✗**)다. 평온했던 구간2에서는 정적 배분이
> 더 나았다. 노출도 71.3% 대 75.7%로 맞춰져 있지 않으므로 그 차이의 일부는
> 노출 차이일 수 있다. **"거의 항상 이긴다"이지 "항상 이긴다"가 아니다.**

> **노출을 맞추지 않은 비교는 무의미하다.** 주식을 덜 들면 낙폭은 당연히 준다.
> 정적 배분 3점(50/50·70/30·90/10)으로 기준선을 만들면 낙폭이 노출에 거의
> 선형이므로, 그 선 대비 얼마나 낮은지를 봐야 한다.

### ② 그 우위는 변동성 관리에서 온다

레이어를 하나씩 꺼서 갈랐다. 노출 보정 후 우위:

| | 2011~ | 2006~ |
|---|---|---|
| 둘 다 끔 | 1.1%p | -0.5%p |
| 추세만 (변동성 끔) | 5.5%p | 4.4%p |
| **변동성만 (추세 끔)** | **10.2%p** | **13.4%p** |
| 현재 스택 (둘 다) | 10.3%p | 15.9%p |

- **"둘 다 끔"이 정적 배분과 같다.** 두 레이어를 빼면 남는 게 없다 — 분석 틀이
  맞다는 검증이다.
- **변동성 관리 단독이 전체의 99%와 84%다.**
- **추세의 기여는 구간마다 부호가 뒤집힌다.** 추세를 껐을 때 MDD 변화가 구간별로
  +1.77 / −0.06 / +1.43 / −0.45 (**2/4 ✗**)다. 폭락 구간에서는 1.4~1.8%p를 지키고
  평온 구간에서는 오히려 조금 해롭다. **평균값을 근거로 삼을 수 없다.**
  - 다만 **가중치 사이의 순서는 견고하다.** 1이 0.5보다 낙폭이 낮고(4/4),
    2보다 Sharpe가 높다(4/4). "추세를 쓴다면 1이 맞다"와 "추세를 쓰는 게 낫다"는
    서로 다른 주장이고, 앞의 것만 뒷받침된다.
- 반면 **변동성 관리를 끄면 MDD가 모든 구간에서 나빠진다** (+18.5 / +1.3 / +12.9
  / +2.5, **4/4**). 이쪽만 방향이 확실하다.

### ③ 변동성 목표 0.15가 최적점이다

목표값을 바꿔가며 노출 보정 우위를 재면 안쪽에 봉우리가 있고, 그 위치가 0.15다.

| volTarget | 2006~ 우위 | 2011~ 우위 |
|---|---|---|
| 끔 | 4.42%p | 5.51%p |
| 0.10 | 13.97%p | 8.94%p |
| **0.15** | **15.92%p** | **10.34%p** |
| 0.20 | 14.39%p | 10.39%p |

0.20은 폭락장이 없는 표본에서 겨우 따라붙고(0.05%p 차이) 2008이 들어가면 1.53%p
뒤진다. 0.10은 방어가 과해서 노출을 줄인 대가만큼도 못 건진다(Sharpe 0.471로 최악).

> **0.20은 Sharpe가 두 표본 모두에서 높고(+0.03) 회전율이 27% 낮다.** 그럼에도
> 쓰지 않는 이유는 Sharpe가 수익을 변동성으로 나눈 값이고 이 시스템이 사는 것은
> 낙폭이기 때문이다. 0.20을 고르는 것은 사려던 것을 팔아 주장하지 않기로 한 것을
> 사는 일이다. **목표가 "낙폭 최소화"에서 "위험조정수익 최대화"로 바뀌면 그때는
> 0.20이 맞다.** 그 결정은 백테스트가 아니라 사람이 한다.

### ④ 손절은 거의 발동하지 않으며 폭락 방어에 기여하지 않는다

20년 실데이터에서 손절을 **아예 끈 것**과 현재 12%의 차이:

| | 손절 없음 | 고정 12% | 차이 |
|---|---|---|---|
| MDD (2006~) | 29.861% | 29.831% | **0.03%p** |
| 체결 (20년) | 637건 | 654건 | 17건 |

문턱을 1.5σ로 넓히면 2011~ 표본에서 **모든 지표가 "손절 없음"과 소수점까지
일치한다.** 한 번도 발동하지 않았다는 뜻이다.

구조적 이유가 있다. 변동성 관리와 목표 비중 레이어가 먼저 익스포저를 줄이므로,
포지션이 진입가 대비 12% 빠지기 전에 리밸런싱이 비중을 정리한다. **손절이 닿을
만큼 남아 있는 경우가 드물다.**

구간 분할로 재확인했다. **구간 2·3·4에서 0.85σ와 1.5σ의 차이가 정확히 0**이다 —
그 구간들에서 손절은 한 번도 발동하지 않았다. 여섯 변형 전부 평균 차이가 구간
폭보다 작다(평균/폭 = 0.05~0.31). **문턱을 어떻게 잡든 결과가 바뀌지 않는다.**

거래비용을 올려도 마찬가지다. 손절 없음이 10·30·50bp 전부에서 CAGR·Sharpe·CDaR
모두 12%보다 낫고, 비용이 오를수록 격차가 벌어진다(CDaR −0.33 → −0.79 → −1.36).

> **하락장 대응을 손절 문턱에서 찾지 마라.** 2008 표본에서 벤치마크 대비 26.8%p의
> 낙폭 우위 중 변동성 관리가 13.4%p, 추세가 2.5%p, **손절이 0.03%p**다. 레버는
> 손절이 아니라 변동성 관리에 있고, 그 목표값은 ③에서 확정했다.

`STOP_LOSS_RATE`는 **전략 파라미터가 아니라 표본에 없는 꼬리 위험에 대한 측정
불가능한 보험**으로 분류한다. 20년에 열 번 남짓 발동하고 비용은 CAGR 0.09%p다.
튜닝 대상이 아니다.

### ⑤ 손실 한도로 매매를 멈추면 더 나빠진다

자동 중단은 최대 낙폭을 **29.8% → 51.6%** 로 키웠다. 10% 한도가 모든 변형 중
최악이었다.

원인은 흡수 상태다. 매수는 멈추는데 방어 매도는 계속되니 전액 현금이 되고 다시
들어가지 못한다. 표본의 남은 17년 동안 자산이 얼어붙었다. 청산까지 함께 멈춰도
해결되지 않았다.

**그래서 손실 한도는 경고일 뿐 매매를 바꾸지 않는다.** `.env`의 값은 "중단
기준"이 아니라 "경고 기준"이다.

### ⑥ alpha가 마이너스인 이유의 80%는 결함이었고, 그 결함은 고쳤다

alpha −$1.81을 분해한 실측값:

| 항목 | 금액 | |
|---|---|---|
| 목표 미달 이탈 | **−$1.44** | 결함 |
| 거래비용 | −$0.20 | |
| 종목선택·구간내매매 | −$0.17 | |
| **구조적 현금 드래그** | **+$0.02** | 설계 |

**"현금을 많이 들어서 뒤처졌다"는 통념은 틀렸다.** 설계된 현금 보유의 비용은
사실상 0이었다. 원인은 청산 규칙이 포지션을 비워 목표 비중을 못 채운 것이었고,
2026-08-05에 수정했다.

**이 손실은 회수되지 않는다.** 앞으로의 기준은 "alpha가 회복되는가"가 아니라
"시장 방향에 따라 진동하는가"다.

### ⑦ 무거래 밴드의 방향 비대칭 — 고쳤으나 절반만 덮는다

밴드는 매수·매도에 같은 값(`max($1, 0.05 × 자산)`)을 쓴다. 그런데 **진입은
목표 전체가 결손이라 밴드를 넘고, 이탈은 목표가 내려간 만큼만 초과분이라
밴드에 못 미친다.** 그래서 신호가 한 번 튀어 세운 포지션이 신호가 되돌아와도
남는다.

가격을 고정해 재현했다(`test/rebalance-band-ratchet.test.js`). 비중이 움직이면
원인은 엔진의 주문뿐이다.

| 구간 | 점수 | IWM 목표 | IWM 보유 |
|---|---|---|---|
| 워밍업 | 0 | 0% | 0% |
| 스파이크 | 0.6 | 6% | 6.00% |
| **되돌림 40거래일** | **0.156** | **1.6%** | **6.00%** (체결 0건) |
| 점수 0 복귀 | 0 | 0% | 0% |

**응답은 연속이 아니라 계단이다.** 되돌림 점수 0.09에서 전량 매도, 0.0995에서
전량 유지다. 목표가 0.1%p 움직이는 사이 포트폴리오가 **6%p** 갈리고, 그 사이
어디에도 부분 정리가 없다. 해제 조건은 `보유 − 목표 ≥ 밴드`이므로 점수 ≤ 0.0997,
즉 **"신호가 약해짐"이 아니라 "신호가 부호를 바꿈"이다.**

`market-signal.js`의 배분 보간은 "점수 0.1 변동이 30%p 점프가 아니라 2%p
드리프트가 되고 무거래 밴드 안에 들어와 실제 주문으로 이어지지 않는다"는 이유로
도입됐다. 절반만 맞다. 작은 드리프트는 흡수되지만, **이미 밴드를 넘어 세워진
포지션의 해체는 흡수되지 않는다. 절벽은 배분 층에서 사라진 게 아니라 체결
층으로 옮겨갔다.**

> **이것은 통계가 아니라 코드의 결정론적 성질이다.** 표본 크기와 무관하게 참이고,
> 백테스트로는 볼 수 없다 — 백테스터는 감성을 `null`로 넣고(`backtest-engine.js`),
> 남는 200일선은 목표를 하루 만에 밀어올릴 만큼 빠르지 않다. **밴드 폭 문제와
> 밴드 방향 비대칭 문제는 다른 문제다.**

**2026-08-07에 `TARGET_DRIFT_CAP = 2`를 기본값으로 켜서 고쳤다.** 되돌림 40거래일
뒤 IWM이 6%에 갇히는 대신 목표 1.6%에 안착한다. 가격표는 **CAGR 연 0.152%p**다.

> **단, 덮는 범위가 설계 의도의 절반이다.** 문턱은 `초과분 ≥ 목표 × 배수`이지
> `보유 ≥ 목표 × 배수`가 아니다(`paper-engine.js`의 `exitBand()`). 그래서 교차점은
> **밴드 / 배수 = 2.5%**이고, 08-07 기록에 적힌 `밴드/(배수−1) = 5%`가 아니다.
> 보유 6%에서 실측 발동 경계는 **목표 2% 부근**이다 — 목표가 2.5%로 되돌아오면
> 여전히 6%에 갇힌다(`test/rebalance-band-ratchet.test.js`).
>
> 08-05 IWM이 고쳐진 것은 목표가 1.6%로 되돌아와 **간신히** 그 안에 들었기
> 때문이다. 채택 근거였던 단위 테스트와 CAGR 가격표는 둘 다 이 좁은 버전을 잰
> 것이라 그대로 유효하다. 틀린 것은 "왜 1.25가 아니라 2인가"를 설명하던 교차점
> 공식뿐이고, 실측에서도 2가 1.25를 모든 축에서 이겼다.
>
> **넓은 버전(교차점 5%)은 측정한 적이 없다** — §5 참조.

큰 포지션은 그대로다. VTI(70%)·SCHD(19.5%)는 목표가 교차점 2.5%보다 훨씬 커서
자산 대비 밴드 5%가 먼저 걸린다. §2에서 확정한 밴드 폭은 건드려지지 않았다.

### ⑧ 익스포저 상한을 올려도 수익을 사지 못한다 — 기각

익스포저 배수의 상한이 1로 박혀 있어 변동성 관리가 한 방향으로만 동작한다.
시끄러우면 줄이고, 조용해도 기본 배분 이상으로는 늘리지 않는다. **아낀 낙폭을
노출로 되쓰면 CAGR을 살 수 있는가**를 2026-08-07에 쟀다. **못 산다.**

실데이터 VTI·IWM 5000일(2008 포함), 10bp:

| | CAGR% | Sharpe | MDD% | CDaR5% | 평균노출% | 회전율/년 |
|---|---|---|---|---|---|---|
| **현재 (상한 1.0)** | **5.993** | **0.568** | **29.896** | **24.512** | 75.559 | **2.714** |
| 상한 1.2 | 5.802 | 0.543 | 30.386 | 25.038 | 77.793 | 3.301 |
| 상한 1.4 | 5.811 | 0.544 | 30.386 | 25.038 | 77.797 | 3.305 |
| 상한 1.6 | 5.811 | 0.544 | 30.386 | 25.038 | 77.798 | 3.304 |
| 정적 70/30 | 6.425 | 0.517 | 44.831 | 32.820 | 71.335 | 0.229 |
| 정적 90/10 | 8.049 | 0.519 | 54.093 | 40.886 | 92.413 | 0.219 |

**세 지표가 모두 나빠지고 셋 다 4/4다.** 구간별 차이는 CDaR +0.957 · MDD +0.696 ·
Sharpe −0.040이고 방향이 한 구간도 어긋나지 않는다. 수익이 내려가면서 낙폭이
같이 커졌으므로 **맞교환이 아니라 순손해다.**

원인은 얻은 것이 작기 때문이다. **노출은 +2.2%p밖에 안 늘었다**(75.6 → 77.8).
상한이 여는 11%p는 변동성이 목표 15% 아래인 날에만 쓰이는데 그런 날이 소수다.
1.2·1.4·1.6이 사실상 같은 것도 그래서다 — 포화점이 배분표에 있지 상한에 없다.
**적게 얻고 대가(회전율 +21.6%, 낙폭 4/4 악화)는 다 치렀다.**

> **왜 낙폭까지 나빠지는지는 재지 않았다.** 변동성이 군집하므로 조용한 구간의
> 끝이 변동성 급등이고, 상한을 올리면 가장 많이 든 상태로 국면 전환을 맞는다는
> 설명이 가능하다. **가설이고 이번 실험이 잰 것이 아니다.**

**얻은 것이 하나 있다 — 정적 배분 사다리에서 우리 위치가 처음으로 나왔다.**
정적 70/30 → 90/10의 기울기는 MDD 1%p당 CAGR 0.175%p다. 그 선을 MDD 29.9%까지
내리면 CAGR 3.81%인데 우리는 **5.99%로 +2.19%p 위**에 있다. 더 직접적으로는,
**정적 70/30보다 주식을 더 들고도(75.6% 대 71.3%) 낙폭이 14.9%p 낮고 수익은
0.43%p 낮다.**

> **단, 그 선은 크게 외삽한 것이다.** 측정점은 MDD 44.8~54.1에 있고 우리 위치는
> 29.9다. 기울기가 그 구간까지 선형이라는 보장이 없다. 방향은 믿되 +2.19%p라는
> 크기는 믿지 마라.

`MAX_EXPOSURE` 손잡이는 남기되 **기본값 1로 둔다.** 배분표(`ALLOCATIONS`) 자체를
올리는 것은 이 실험이 답한 문제가 아니다 — 그것은 노출을 규칙으로 늘리는 게
아니라 전략의 기본 자세를 바꾸는 별개의 결정이다.

### ⑨ 거시 층은 상수인 동안 신호가 아니라 노출 다이얼이다

FRED 거시 층은 점수 하나로만 배분에 들어간다(`allocationForScore`). 그 점수의
범위는 **−7 ~ +3.5**로 추세(±1)·감성(±2)보다 크다 — **범위 기준으로 가장 큰
입력인데 가중치 손잡이가 없다.** 그리고 점수는 주식 비중으로 이렇게 번역된다:
0에서 90%, −1.5에서 60%. **점수 1당 주식 20%p다.**

실데이터 VTI·IWM 5000일에서 상수를 쓸었다:

| 거시 점수 | 평균노출% | CAGR% | MDD% | CDaR5% | Sharpe |
|---|---|---|---|---|---|
| +0.5 | 79.0 | 6.121 | 31.039 | 24.963 | 0.548 |
| 0 (백테스트 기본) | 75.6 | 5.993 | 29.896 | 24.512 | 0.568 |
| **−0.5 (실운영 값)** | **71.2** | **5.454** | **27.076** | **22.225** | 0.557 |
| −1.0 | 66.5 | 4.993 | 25.504 | 20.584 | 0.550 |
| −1.5 | 59.8 | 4.535 | 22.761 | 17.683 | 0.545 |

**CAGR과 MDD는 노출을 따라 4/4로 움직이는데 Sharpe는 부호일치가 전부 ✗다**
(3/4 · 3/4 · 2/4 · 3/4). §3 규칙대로면 Sharpe의 방향은 주장할 수 없다.
**즉 이 층은 위험을 조정해 주는 것이 아니라 위험선 위에서 어느 점에 설지만
고른다.** 상수인 동안 거시 층은 신호가 아니라 파라미터다.

교환비는 CAGR 1.586%p ↔ MDD 8.278%p, **MDD 1%p당 CAGR 0.192%p**다.
⑧의 정적 사다리 기울기 0.175와 같은 자릿수다 — **주식을 덜 드는 것과 사실상
구별되지 않는다.**

> **백테스트와 실운영의 노출이 달랐다 — 2026-08-08에 해소됐다(⑫).**
> 백테스트 기본은 macro=0(노출 75.6%)인데 실운영은 −0.5(71.2%)였다. 아래 수준값은
> 그 시절의 것이다. 가중치를 0으로 내리면서 둘이 같아졌다. 밴드·드리프트캡·변동성 목표처럼 상대 비교인 판정은 뒤집히지
> 않겠지만, **수준값은 다르다** — 실운영 기대치는 CAGR 5.99%가 아니라 **5.45%**,
> MDD 29.9%가 아니라 **27.1%**다. **⑫ 이후로는 이 보정이 필요 없다.**


### ⑩ 거시 층은 2008년에만 값을 했다 — 타이밍 가치는 주장할 수 없다

⑨는 상수로서의 성격만 봤다. 실제 FRED 값을 **그 시점에 알 수 있었던 것만으로**
되살려(개정 이력 기반, `--macro-source vintage`) 20년을 다시 돌렸다.

되살린 점수는 **움직인다** — 범위 −5 ~ +2.5, 표준편차 1.57, 값이 바뀐 날 142일.
평균은 **+0.153**이다. 실운영이 붙박여 있는 −0.5는 20년 분포에서 비관 쪽 끝이다.

**노출을 맞춘 비교.** vintage의 평균 노출 73.269%는 상수 −0.5(71.214)와
0(75.559) 사이, 상수 −0.264 자리다. 그 지점으로 보간한 상수와 비교하면:

| | 되살린 거시 | 같은 노출의 상수 | 차이 |
|---|---|---|---|
| CAGR% | 5.722 | 5.709 | +0.01 |
| **MDD%** | **25.920** | 28.410 | **−2.49** |
| **CDaR%** | **21.001** | 23.307 | **−2.31** |
| Sharpe | 0.551 | 0.562 | −0.011 |

전체 표본만 보면 값을 한 것처럼 보인다. **구간을 나누면 무너진다.**
가장 가까운 상수 −0.5 대비 MDD 차이(양수 = vintage가 나음):

| 구간1 | 구간2 | 구간3 | 구간4 | 부호일치 |
|---|---|---|---|---|
| **+1.156** | −2.135 | −4.463 | −1.671 | **3/4 ✗** |

CDaR도 같은 모양이다(+1.298 / −1.749 / −3.326 / −0.953, 3/4 ✗).

> **결정적인 확인.** 전체 표본 MDD 차이(27.076 − 25.920 = **1.156**)가 구간1
> 값과 소수점까지 같다. 위 표의 "2.5%p 우위"는 **전부 2008년 한 구간에서 나온
> 것**이고 나머지 15년은 오히려 손해였다.

4/4가 나온 것은 상수 −1.0·−1.5 대비인데 그쪽은 노출이 66.5%·59.8%로 한참 낮아
비교 대상이 아니다. 그 표에는 부호검정이 12개 있고 우연한 4/4 기대값이 1.5개다(§3).

**사전에 정한 기준(§5-1의 3관문)대로 타이밍 가치를 주장하지 않는다.**
추세 층(②)과 정확히 같은 모양이다 — 폭락에서 벌고 평온에서 조금씩 잃으며
부호가 갈린다. 그리고 **2008년은 폭락 한 번이다. n=1로 "때를 맞춘다"고 말할 수 없다.**

> **되살리기 자체는 검증했다.** Sahm을 실업률 vintage에서 직접 계산한 값이
> FRED `SAHMREALTIME`과 83개월 중 78개월 소수 둘째자리까지 일치하고 평균 차이가
> 0.003이다. 가장 어긋난 달이 0.07인데, `SAHMREALTIME`이 각 달을 최초 발표치로
> 고정하는 반면 우리는 "그날 알려져 있던 값"을 쓰기 때문이다. **백테스트
> 목적으로는 우리 쪽이 맞다** — 그날의 에이전트가 실제로 봤을 값이 그것이다.
> 다만 Sahm 문턱이 0.35·0.50이라 0.07 오차가 경계에서 판정을 뒤집을 수 있다.

### ⑪ 감성 층의 점수는 키워드 두 번이 지배한다

2026-08-07 `npm run sentiment:status` 실측:

```
sourceCounts   FED_RSS 30 · BLUESKY 229 · OPINION_RSS 100   (합 359)
warning        "fetch failed"            ← GDELT가 빠져 있다
officialSentiment  −0.744   30건 중 "시장 표현 2개", 약세 신호 ["miss"]
opinionSentiment   +0.112   329건 중 표현 102개
opinionWeight      0.3      → 공식 층이 0.7을 먹는다
통합               −0.546   기여도 −0.328 → 주식 약 6.5%p 감소
```

**두 가지가 겹쳐 있다.**

**① 빠른 소스가 죽었다 — 원인은 우리 타임아웃이었다(2026-08-08 확인).**
GDELT는 이벤트에 반응하는 유일한 소스인데 `The operation was aborted due to
timeout`으로 실패하고 있었다. **차단이 아니라 우리가 건 10초 시한이 짧았다** —
RSS·Bluesky는 정적 파일에 가까워 1초 안에 오지만 GDELT의 doc API는 넓은 OR
질의를 매번 훑어 10~30초가 걸린다. 같은 잣대를 대면 느린 소스만 골라 죽인다.
GDELT만 30초로 늘렸다. 요청은 병렬이라 사이클이 기다리는 시간은 가장 느린
하나뿐이고, 15분 주기에서 30초는 감당할 수 있다.

그 사이에 쌓인 표본(08-06~08-07)은 **GDELT가 빠진 구성**이다. 소스 지문이
남아 있으므로 판정할 때 구간을 갈라야 한다.

**② 점수를 표현 두 개가 지배한다.** 공식 층이 가중치 0.7을 먹는데, 그 −0.744는
**30개 기사에서 찾은 시장 표현 2개**에서 나온다. 약세 신호는 `"miss"` 하나다.
반면 329건을 본 의견 층은 +0.112이고 가중치가 0.3이다.

> **"miss"가 두 번 걸린 것이 주식 비중을 6.5%p 깎고 있다.**
> PAPER라 손해는 없지만, 지금 쌓이는 운용 기록은 "그 상태로 돈" 기록이다.

**아직 가중치를 건드리지 않는다. 표본 수집도 예정대로 계속한다.**

이것은 관측이지 판정이 아니다. 판정 기준은 §3에 있고 10거래일을 채워야 한다.
중간 관측을 보고 층을 끄는 것은 **결과를 보고 골대를 옮기는 것**이고, 거시 층에서
"2008년 우위는 실측이지만 n=1이라 주장하지 않는다"고 한 것과 앞뒤가 맞지 않는다.
§3의 "값이 움직이는가" 관문이 이미 앞에 있으므로, 멈춘 값이 "정보를 담고 있다"로
읽힐 위험은 닫혀 있다.

> **대신 표본이 섞이는 것을 막는다.** 수집 도중 GDELT가 살아나면 그 전후는
> **다른 구성의 감성**이고, 둘을 한 자기상관으로 묶으면 아무것도 재지 않는 값이
> 된다. 그래서 스냅샷마다 살아 있던 소스의 지문(`sourceFingerprint`)을 남기고,
> 표본 안에서 구성이 바뀌면 `paper:signals`가 경고한다. **구성이 같은 구간끼리
> 나눠 판정한다.**

### ⑫ 거시 층을 배분에서 뺐다 (2026-08-08 결정)

⑨⑩으로 잴 수 있는 것은 다 쟀고, 남은 것은 사람의 결정이었다. **`MACRO_SCORE_WEIGHT = 0`**.

**결정이 둘로 나뉘어 있었고, 앞의 것은 측정이 답했다.**

| | |
|---|---|
| 값이 움직이는 것을 둘 것인가 | **아니오** — 2008 한 구간만 우위, 나머지 3구간 열세, 평균 −1.78%p (⑩) |
| 그럼 어떤 상수로 둘 것인가 | **0** — 아래 |

0을 고른 이유 셋:

1. **−0.5에 근거가 없다.** 그것은 오늘의 FRED 값일 뿐이고 20년 되살리기 평균은
   **+0.153**이다. 분포의 비관 쪽 끝에 고정할 이유가 없다.
2. **검증 안 된 규칙이 −7까지 튈 수 있다.** Sahm 하나가 −3이고 다 겹치면
   RISK_OFF 바닥(주식 60%)까지 내려간다. 언제 발동할지 모르고, 발동해도 값을
   한다는 증거가 없다.
3. **노출을 줄이려면 명시적인 손잡이로 정한다.** 여섯 개 FRED 문턱의 부산물로
   정해지는 것보다 낫다.

**대가를 적어 둔다.** 평균 노출이 71.2% → 75.6%로 오르고 MDD가 27.1% → 29.9%로
**2.8%p 커진다.** 이 시스템이 사는 것이 낙폭이므로 가벼운 대가가 아니다.
그럼에도 고른 것은, 그 2.8%p를 **근거 없는 상수로 사는 것보다 명시적으로 사거나
안 사는 편이 낫다**고 봤기 때문이다. 줄이고 싶으면 `VOL_TARGET`이나 배분표로 정한다.

> **곁가지 하나가 해결됐다.** ⑨에서 "백테스트는 macro=0인데 실운영은 −0.5라
> 지금까지의 모든 결론이 실제보다 4%p 높은 노출에서 측정됐다"고 적었다.
> **이제 둘이 같아졌다.** 백테스트 기본값과 운영이 일치한다.

**층을 지우지 않았다.** 점수는 계속 계산되고 이벤트 로그에 남는다. 폭락이 한 번
더 쌓이면(n=2) 같은 질문을 다시 물을 수 있어야 하고, `--compare macro`·
`macrotiming`도 그대로 돈다(그쪽은 가중치 1로 잰다).

---

## 2. 현재 파라미터와 그 값인 이유

| 설정 | 값 | 근거 |
|---|---|---|
| `STOP_LOSS_RATE` | **0.12** | 측정 불가능한 꼬리 보험. 어떤 값이든 결과가 같다 (④) |
| `STOP_LOSS_SIGMA` | **꺼짐** | 변동성 배수 손절. 구현돼 있으나 개선 폭이 노이즈 아래라 안 켠다 (④) |
| `TRAILING_*` | **꺼짐** | 개별 종목용 모멘텀 규칙. 지수에서는 회전율만 21배로 키웠다 |
| `MAX_HOLDING_DAYS` | **꺼짐** | 장기 배분에 "시간이 지났다"는 매도 근거가 없다 |
| `MACD_SCORE_WEIGHT` | **0** | 기여 상한이 무거래 밴드보다 작아 주문을 만들 수 없다. 백테스트에서도 재현되는 엣지 없음 |
| `TREND_SCORE_WEIGHT` | **1** | 0.5보다 낙폭이 낮고(4/4) 2보다 Sharpe가 높다(4/4). 양쪽에서 막혀 있다 (②) |
| `VOL_TARGET_ANNUALIZED` | **0.15** | 우위의 원천이자 노출 보정 우위의 최대점 (①③) |
| `MACRO_SCORE_WEIGHT` | **0** | 거시 층을 배분에서 뺐다(2026-08-08 결정). 점수는 계속 기록된다 (⑨⑩⑫) |
| `MAX_EXPOSURE` | **1** | 익스포저 배수 상한. 1.2로 올리면 CAGR·Sharpe·MDD·CDaR가 전부 4/4로 나빠져 기각 (⑧) |
| `TRADE_COST_RATE` | **0.001** (10bp) | 실측 수수료는 현재 **0**이다(아래). 낮추지 않는다 — 이 값은 수수료만이 아니라 슬리피지·환전까지 덮는 가정이고, 그 둘은 아직 재지 않았다 |
| `REBALANCE_BAND_RATE` | **0.05** | 1%·10%와 비교해 확정. 아래 표 참조 |
| `REBALANCE_EXIT_BAND_RATE` | **매수와 동일** | 이탈 밴드 분리 손잡이. 구현돼 있으나 안 켠다 (⑦) |
| `TARGET_DRIFT_CAP` | **2** | 목표 대비 이탈 상한. 방향 비대칭의 해법으로 2026-08-07 채택. 가격 CAGR 0.152%p (⑦) |
| `MIN_POSITION_RATE` | **꺼짐** | 밴드 미만 목표를 0으로. 잡음을 회전율로 바꿔 기각 (§5) |
| `REGIME_CONFIRM_DAYS` | **1** | 1거래일 = 26사이클 |
| 손실 한도 | 경고 전용 | ⑤ 참조 |

**구간 분할로 재검증했다(4구간).** 0.15와 0.20을 직접 뺀 구간별 차이:

| | 구간1 | 구간2 | 구간3 | 구간4 | 부호 |
|---|---|---|---|---|---|
| MDD | −5.28 | −0.96 | −2.25 | −1.04 | **4/4 (0.15 우위)** |
| CDaR | −4.63 | −0.27 | −2.33 | −0.78 | **4/4 (0.15 우위)** |
| Sharpe | −0.014 | −0.118 | −0.054 | −0.053 | **4/4 (0.20 우위)** |

**둘 다 4/4다.** 0.15가 낙폭에서, 0.20이 위험조정수익에서 모든 구간을 이긴다.
노이즈가 아니라 **진짜 맞교환**이다. 우리가 사는 것이 낙폭이므로 0.15다.

**거래비용 10bp 가정의 실측 근거(2026-08-07, 실계좌 2건).**

| 주문 | 체결 | 수수료+세금 | 0.1%였다면 |
|---|---|---|---|
| $2.00 | $1.99 @ $33.52 | **$0** | $0.002 (센트 아래) |
| $10.00 | $9.98 @ $33.59 | **$0** | $0.01 (보였어야 함) |

$10에서도 0이므로 **현재 미국주식 수수료는 0**이다. 다만 `/commissions`가
알려주는 US 요율 행의 `endDate`가 **2026-08-08**이고 값이 `0.1`이다. 면제가
끝나면 10bp가 붙어 **우리 가정과 정확히 일치**한다.

> **그래도 `TRADE_COST_RATE`를 낮추지 않는다.** 이 값은 수수료만이 아니라
> **슬리피지와 환전 비용까지 덮는 가정**이고, 그 둘은 아직 재지 않았다.
> 금액 주문은 시장가라 체결가를 우리가 통제하지 못하므로 슬리피지가 실제
> 비용의 큰 부분일 수 있다. 그것을 재는 것이 11월 병행 운용의 목적이다.

> **참고: 두 주문 모두 장 시작 직후(09:34·09:38 ET)에 냈다.** 그 구간은 스프레드가
> 가장 넓다. 금액 주문 창이 09:30~15:00이므로 우리 실행기는 개장 직후에도 낼 수
> 있는데, **개장 초반을 피하는 것이 나은지는 아직 재지 않았다.**

**밴드 폭 0.05도 구간 분할로 확정했다(2026-08-07).**

| 밴드 | CAGR% | Sharpe | MDD% | 체결 | 회전율/년 |
|---|---|---|---|---|---|
| 1% | 8.097 | 0.759 | 19.194 | 1675 | 3.313 |
| **5%** | **8.165** | **0.773** | **18.735** | **323** | **1.655** |
| 10% | 8.299 | 0.752 | 19.743 | 130 | 0.915 |

10%는 Sharpe가 1% 대비 **4/4로 나쁘다**. 넓혀서 사는 게 없다.
1%는 MDD 4/4로 이기지만 **0.276%p**다 — 추세(폭락 구간 1.4~1.8%p)의 1/5,
변동성 관리(10.2~13.4%p)의 1/40이다. 대가는 회전율 2배, 체결 5.2배.
08-06에 손절 3%를 기각한 근거(회전율 +35~41%에 CDaR 1.0~1.6%p)와 같은 형태인데,
**우위는 그때의 1/4~1/6이고 회전율 증가는 3~6배 크다.**

> **주의: 이 표의 "1%"는 표본 전반에 걸친 1%가 아니다.** `fundedUsd = 67.05`에
> `minOrderUsd = $1`이라 `tradeBand()`가 `max($1, rate × 자산)`을 쓴다. 시작
> 시점 1% = $0.67이 $1로 막혀 실효 1.49%이고, 자산이 커지며 풀린다.
> 자산이 작을 때는 **밴드율 1.5% 미만을 표현할 수 없다.**

> **`VOL_TARGET_ANNUALIZED`를 0.20으로 올리자는 제안은 2026-08-06에 기각됐다.**
> 2011~ 표본만 보면 Sharpe·수익률·회전율이 모두 나아 보이지만, 2008을 넣으면
> 낙폭이 5.28%p 나빠진다. 미리 정한 허용치는 3%p였다. 상세는
> `develope-log/2026-08-06_변동성목표-확정.md`.

---

## 3. 판정 기준 — 결과를 보기 전에 정한 것

사후에 해석을 고르지 않기 위해 미리 박아둔 규칙이다. **결과가 마음에 안 든다고
바꾸지 않는다.**

**뉴스 감성이 신호인가 잡음인가** — **일별** 1차 자기상관으로 가른다.
**단, 자기상관보다 앞서는 관문이 하나 있다.**

> **① 값이 움직이는가 (2026-08-07 추가, 표본이 차기 전).**
> 일별 표준편차가 **0.01 미만이면 판정을 보류한다.** 감성 원점수는 −1~+1
> 범위이므로 0.01은 전체 폭의 0.5%다. 그보다 덜 움직이면 "안정적"이 아니라
> **수집이 고장난 것**으로 본다.
>
> **이 관문이 없으면 결론이 정반대로 뒤집힌다.** 멈춘 값은 자기상관이 1에
> 가깝고, 아래 규칙은 0.3 이상을 "값이 이어짐 → 가중치 유지"로 읽는다.
> 즉 **고장을 가장 강한 신호로 읽는다.** 거시 층에서 "구간이 대부분 정확히
> 0이면 규칙이 발동하지 않은 것이고 부호가 일치해도 효과가 아니다"라고 한 것과
> 같은 함정이다.
>
> **실제로 지금 그 상태다.** 08-06 14:38부터 일별 표준편차가 0.001이다.
> 다만 **원인은 캐시 고장이 아니다** — `fetchedAt`은 갱신되고 점수도 미세하게
> 움직인다. 파이프라인은 돈다. 아래 ⑪을 보라.

> **간격을 2026-08-07에 "일별"로 못 박았다. 표본이 차기 전이고 숫자를 보기
> 전이다.** 08-06에 정한 규칙은 문턱(0.1/0.3)과 표본 수(10)만 적었고 간격이
> 비어 있었다. 그 빈칸을 채운 것이지 규칙을 바꾼 것이 아니다 — 문턱도 표본
> 수도 그대로다.
>
> 스냅샷 단위로 재면 안 되는 이유가 둘이다. **뉴스 캐시가 60분이라 연속 스냅샷이
> 1시간 간격이고, 같은 뉴스 사이클 안이라 자기상관이 부풀려진다.** "1시간 뒤에도
> 값이 비슷하다"로 0.3을 넘기면 정보를 담았다고 잘못 판정한다. 그리고 장 마감
> 마지막 스냅샷과 다음 날 첫 스냅샷 사이는 17.5시간인데 같은 1차 시차로 취급된다.
>
> 무엇보다 **실제로 문제가 된 것이 일간이다** — 08-05 +0.108에서 08-06 −0.545로
> 하루 만에 부호가 뒤집혀 IWM 편입을 촉발했다. 매매 판단이 일어나는 시간축도
> 그쪽이다. 스냅샷 단위 값은 참고로 함께 내되 판정에 쓰지 않는다
> (`test/signal-history.test.js`가 두 값이 정반대로 나올 수 있음을 고정한다).
>
> **표본 10건은 이제 10거래일이다.** 스냅샷 기준보다 오래 걸린다.

| 자기상관 | 판정 |
|---|---|
| ≥ 0.3 | 값이 이어짐 → 가중치 유지 |
| 0.1 ~ 0.3 | 애매 → 표본 추가 |
| ≤ 0.1 | 난수와 구분 안 됨 → 가중치 축소·평활 |

표본 10거래일 미만이면 **숫자를 내지 않는다.** 적은 표본의 통계는 근거처럼 쓰인다.

**실데이터 백테스트의 해상도 — 문턱을 선언하지 말고 측정한다.**

경로가 하나뿐이라 표준오차가 없다. 그래서 **표본을 겹치지 않는 구간으로 나눠
같은 비교를 반복하고, 구간별 차이의 폭을 노이즈로 읽는다.**

```
npm run backtest -- --compare <종류> --source cache --blocks 4
```

**부호일치가 1차 판정이다.**

| 구간별 부호 | 판정 |
|---|---|
| 4/4 일치 | 방향이 실재한다. 크기는 국면에 따라 다를 수 있다 |
| 갈림 (✗) | **전체 표본의 숫자를 믿지 마라.** 방향조차 표본이 정한다 |
| 대부분 정확히 0 | 규칙이 발동하지 않았다. 부호가 일치해도 효과가 아니다 |

**폭이 크다고 노이즈인 것은 아니다.** 구간마다 시장 국면이 다르므로, 실재하는
효과도 폭락 구간에서 크고 평온 구간에서 작다. 변동성 관리의 MDD 효과는 구간별로
−18.5 / −1.3 / −12.9 / −2.5인데, 폭이 17.3이어도 **부호가 4/4라 실재한다.**
폭은 효과의 존재가 아니라 **일반화 가능성**을 알려준다.

노이즈 크기는 고정값이 아니라 **그 변형이 거래를 얼마나 바꾸는지에 비례한다.**
2026-08-06 손절 비교에서 실측:

| 회전율 차이 | CDaR 폭 | Sharpe 폭 |
|---|---|---|
| ±1% | 0.03~0.05 | 0.003~0.005 |
| +6% | 1.2 | 0.024 |
| +44% | **2.9** | **0.084** |

그래서 하나의 문턱("Sharpe 0.01")으로는 잡을 수 없다. 거래를 크게 바꾸는 변형일수록
더 큰 차이를 요구해야 한다.

**부호검정을 여러 개 동시에 돌리면 4/4는 저절로 나온다.** 부호가 동전이면
4/4 확률이 1/8이다. 변형 4개 × 지표 3개 = **12개를 한 표에서 보면 기대값이
1.5개**이므로, 4/4가 한둘 나온 것은 우연과 구분되지 않는다.

| 한 표의 검정 수 | 우연히 나올 4/4 기대값 |
|---|---|
| 3 (변형 1개) | 0.4 |
| 6 (변형 2개) | 0.75 |
| 12 (변형 4개) | **1.5** |

**규칙을 바꾸는 것이 아니라 함께 읽는 것이다.** 부호일치는 여전히 1차 판정이되,
**변형을 여러 개 늘어놓은 표에서는 4/4 하나를 채택의 근거로 삼지 않는다.**
그럴 때 기대야 할 것은 실험 전에 정한 기각 조건이 발동했는가다.
그리고 결정론적 성질(코드가 그렇게 동작하는가)은 애초에 이 표가 답할 문제가 아니다.

**주의: 두 표본(2006~·2011~)은 독립이 아니다.** 2011~2026이 2006~2026 안에 통째로
들어 있다. "두 표본에서 일관되다"는 관측 두 개가 아니라 하나 반이다. 구간 분할이
이것을 대체한다.

**벤치마크는 우리 행동에 따라 움직이지 않는다.** "방어 중일 때는 alpha를 면제하자"는
제안은 거절했다. 신호가 틀려서 잘못 방어한 날에 벌칙이 면제되면, 방어가 해로운
국면을 영영 관측할 수 없다.

---

## 4. 아직 모르는 것

- **뉴스 감성 층의 가치.** 백테스트에 넣을 수 없다 — 그날의 GDELT·Bluesky·RSS
  수집창은 지나가면 복원되지 않는다. 2026-08-06부터 운영 로그에 축적 중이며,
  스냅샷 10건이 모이면 위 기준으로 판정한다.
  **현재 상태와 그 구조는 ⑪에 적었다.** 요약하면 빠른 소스(GDELT)가 죽어 남은
  것이 전부 느린 피드이고, 점수는 공식 층 30건에서 찾은 **표현 2개**가 지배한다.
  이 상태로 10거래일을 채우면 자기상관이 1에 가깝게 나와 "정보를 담고 있다"는
  정반대 결론이 나므로, §3의 "값이 움직이는가" 관문이 먼저 걸린다.
  08-04~06의 관측은 아래와 같았다: 원점수가 −0.216 → +0.108 → **−0.545**로 이틀 연속
  부호를 뒤집었고 두 번째 폭(0.653)이 첫 번째(0.324)의 두 배다. 같은 기간 추세는
  0.974에 붙박이였다. 08-05의 +0.108이 IWM 편입을 촉발했고, 08-06의 −0.545로
  목표는 1.6%로 내려갔으나 **보유는 5.5%에 그대로 남아 있었다**(⑦ — 08-07에
  고쳤다. 앞으로 같은 경로는 목표에 안착한다).
  정황 하나: 359건 중 Bluesky가 229건(64%)이고 GDELT는 429로 실패했다. 감성이
  사실상 한 출처에 실려 있는 것이 일간 급변의 기계적 원인일 수 있다. 가설이다.
  **표본 10건 미만이므로 숫자를 내지 않는다.** 08-06 기준 2건.
- **FRED 거시 층이 때를 맞추는가.** 상수로서의 성격은 ⑨에서 판정했다 — **신호가
  아니라 노출 다이얼이다.** 남은 것은 20년 동안 실제로 움직인 값이 때를 맞췄는지이고,
  도구와 판정 기준은 §5-1에 있다. 이 층은 감성과 달리 **잴 수 있다** — FRED는
  개정 이력을 API로 그대로 주므로 그 시점 값을 되살릴 수 있다.
- **매수 체결가가 장중 고점에 쏠리는지.** 08-05에 VTI 체결가가 종가보다 0.65%
  높았다. 단일 관측이라 판단하지 않는다. 이벤트 로그에 사이클별 가격을 쌓고 있다.

---

## 5. 다음 실험

> **익스포저 상한은 2026-08-07에 재고 기각했다 — ⑧.** "노출을 늘려 수익을
> 산다"는 경로는 닫혔다. 남은 후보는 아래 셋과, 성격이 다른 넷째다:
> **배분표(`ALLOCATIONS`) 자체를 올리는 것.** 그것은 규칙으로 노출을 늘리는 게
> 아니라 전략의 기본 자세를 바꾸는 결정이므로 백테스트가 아니라 사람이 정한다.
> 판단에 필요한 환율은 ⑧에 있다 — 정적 사다리 기울기 MDD 1%p당 CAGR 0.175%p.

1. **감성 자기상관 판정** — `npm run paper:signals`. **10거래일** 이후.
   판정은 일별로 하고 간격은 08-07에 확정했다(§3). 그 앞에 관문이 둘 더 있다 —
   소스 구성이 섞였는가, 값이 움직이는가(⑪·§3).

2. **밴드 방향 비대칭의 남은 절반.** ⓒ를 켜서 교차점 2.5%까지는 덮었다(⑦).
   목표가 2.5~5%로 되돌아오는 경로는 아직 갇힌다. 덮으려면 `exitBand()`의
   문턱을 `초과분 ≥ 목표 × (배수−1)`로 바꿔 교차점을 5%로 올려야 하는데,
   **그 변형은 백테스트로 잰 적이 없다.** 지금 있는 숫자는 전부 좁은 버전의
   것이다. 재기 전에는 바꾸지 않는다.

   > 이 구간이 실제로 밟히는지부터 확인하는 편이 싸다. 이벤트 로그에 IWM 목표가
   > 2.5~5%에 머문 사이클이 있는지 세면 된다. 없으면 측정 자체가 불필요하다.

3. **`bandshapecost` 교차** — ⓒ의 회전율 증가는 +12.3%로 손절 3%(+35~41%)의
   1/3이지만, 덤이던 CDaR 우위가 30·50bp에서 살아남는지는 안 봤다.
   사라져도 채택 근거는 안 흔들린다. "CAGR을 더 내고 산다"가 될 뿐이다.

<details>
<summary>채택 당시 후보 비교 (2026-08-07) — 실데이터 3717일, 기준선 대칭 5%</summary>

   | 후보 | 되돌림 후 IWM | 진동 5회 매수/매도 | CAGR% | 회전율 | 부호일치 |
   |---|---|---|---|---|---|
   | 현재 | 6.00% (목표 1.6%) | 1 / 0 | 8.165 | 1.655 | — |
   | ⓐ `MIN_POSITION_RATE` | 0.00% | **5 / 5** | 8.012 | +17.7% | 4/4 없음 |
   | ⓑ `REBALANCE_EXIT_BAND_RATE` | 1.60% | 1 / 1 | 7.710 | **+32.6%** | MDD 4/4 |
   | **ⓒ `TARGET_DRIFT_CAP` = 2** | **1.60%** | 1 / 1 | 8.013 | +12.3% | CDaR 4/4 |

   **ⓐ 기각** — 잔여물을 회전율로 바꾼다. 목표가 밴드를 넘나들면 0%와 6% 사이에
   중간이 없어 매번 전량 매매가 된다. 4/4가 하나도 없으면서 대가만 치른다.

   **ⓑ 기각** — MDD 4/4는 이 표에서 제일 강한 낙폭 결과다. 그러나 08-06에 손절
   3%를 기각한 근거(회전율 +35~41%에 CDaR 1.0~1.6%p)와 같은 자로 재면 ⓑ는
   회전율 +33%에 CDaR 2/4 ✗로 **이미 기각한 것보다 나쁘다.** 게다가 ⓒ와
   전체표본 MDD가 사실상 같은데(18.194 대 18.203) CAGR은 3배 낸다.

   **ⓒ 채택.** 1.25 대비 2가 모든 축에서 나았다 — CAGR 8.013 대 7.951,
   회전율 1.859 대 1.906, 부호일치 4/4 대 2/4.

   **채택 근거는 CDaR 4/4가 아니다.** 그 표에는 부호검정이 12개 있고 우연
   기대값이 1.5개다(§3). 효과는 단위 테스트가 결정론적으로 확정했고, 백테스트에
   물은 것은 **"고치면 얼마를 잃는가"뿐이며 답은 CAGR 연 0.15%p**다.

   > **당시 적은 교차점 설명은 틀렸다.** `밴드/(배수−1)`은 문턱이 보유액 기준일
   > 때의 식이고, 코드는 초과분 기준이라 실제 교차점은 `밴드/배수 = 2.5%`다.
   > 위 표의 숫자는 전부 이 좁은 버전을 잰 것이므로 그대로 유효하다. 1.25가
   > 진 이유도 바뀌지 않는다(교차점 4%로 여전히 2보다 넓다). 상세는 ⑦.

   상세는 `develope-log/2026-08-07_밴드폭-확정과-방향비대칭.md`와
   `develope-log/2026-08-07_드리프트캡-채택과-교차점-정정.md`.

</details>

---

## 6. 규율

- **파라미터를 바꾸기 전에 `npm run backtest`로 먼저 잰다.** 값부터 고치지 않는다.
  비교 종류: `exit`, `macd`, `band`, `bandshape`, `cost`, `strategy`, `vol`, `source`,
  `stop`, `stopcost`, `trend`, `exposure`, `macro`, `macrotiming`.
- 실데이터는 `npm run backtest:fetch` 후 `--source cache`. TwelveData 키가 있는
  배포 호스트에서만 동작한다. 2008년을 포함하려면 `--symbols VTI,IWM`
  (SCHD는 2011년 상장이라 표본을 자른다).
- **낙폭 비교는 반드시 노출을 맞춘 뒤에 한다.**
- 20일치 PAPER 운용으로는 경로 의존 규칙의 효과를 노이즈와 구분할 수 없다.
  구조적 결함은 통계가 아니라 코드의 결정론적 성질이므로 표본 크기와 무관하게 참이다.

---

## 7. 진단 도구

| 명령 | 답하는 질문 |
|---|---|
| `npm run paper:alpha` | alpha가 설계된 비용인가 결함인가 |
| `npm run paper:signals` | 뉴스 감성이 신호인가 잡음인가 |
| `npm run paper:events` | 그때 무엇을 보고 무엇을 했는가 |
| `npm run backtest -- --compare <종류>` | 이 파라미터를 바꾸면 어떻게 되는가 |
| `npm test` | 구조가 여전히 그렇게 동작하는가 |

**백테스트로 답할 수 없는 질문이 있다.** 백테스터는 감성을 `null`로 넣고 거시를
상수로 고정하므로, 그 두 층이 만드는 경로는 아예 밟히지 않는다. 빠른 신호가
만드는 결함(⑦의 밴드 래칫이 그 예다)은 **테스트로 잡는다** — 코드의 결정론적
성질은 표본이 아니라 재현으로 증명된다.

리포트에는 **기준선이 둘** 있다.

- `초과성과(alpha)` — VTI 100% 대비. 시장을 이겼는가.
- `신호 초과성과` — 같은 비중을 신호 없이 들었을 때 대비. **신호 레이어의 순수한 성적.**

둘 다 필요하다. 앞의 것만 보면 "설계된 위험 감축의 대가"와 "신호가 값을 못 함"이
구분되지 않는다.

---

## 관련 문서

- `README.md` — 설정과 사용법
- `trading_method.md` — 매매 방식 상세 명세
- `file_structure.md` — 파일별 역할
- `develope-log/` — 날짜별 개발 기록. **확정된 기록이라 고치지 않는다**
