# Toss API-based Investment Agent

> 최종 갱신: 2026-08-08
>
> 이 문서는 **지금의 TAPIoca**를 설명합니다. 날짜별 경위는
> [`develope-log/`](develope-log/), 전략의 현재 결론은
> [`STRATEGY.md`](STRATEGY.md)에 있습니다.

TAPIoca는 토스증권 Open API로 포트폴리오와 시장 데이터를 분석하고, 정해진 투자
원칙에 따라 **가상 매매(PAPER Trading)** 로 전략을 검증하는 개인 투자 에이전트
실험 프로젝트입니다.

프로젝트 이름 **TAPIoca**는 **Toss API**를 줄인 `TAPI`와 친숙한 단어인 `Tapioca`를
결합해 만들었습니다.

판단에 쓰는 데이터:

- 토스증권 계좌 및 시장가격 데이터
- FRED 미국 거시경제 지표 (개정 이력 포함)
- Federal Reserve 공식 발표, GDELT·Google News 경제뉴스
- 공개 Bluesky 계정, Substack·개인 블로그 RSS
- 200일 이동평균 기반 장기 추세
- 최근 실현 변동성
- 일봉 종가 기반 MACD 보조 신호
- PAPER Trading 누적 거래와 이벤트 로그
- 고정 벤치마크 둘 (VTI 단순보유 · 위험을 맞춘 정책믹스)

주요 사용자 인터페이스는 **Telegram**입니다.

---

# 🏗 현재 구현 상태

## PAPER 검증 + 실주문 실행 계층

**2026-08-07에 실계좌로 첫 주문을 냈습니다.** 다만 **자동으로 주문이 나가지는
않습니다.**

| | 상태 |
|---|---|
| 예약 실행기(15분 주기) | **PAPER 전용** — PAPER 모드가 아니면 실행을 거부합니다 |
| 실주문 | **사람이 직접** `npm run live:probe -- --confirm`을 칠 때만 |
| 실주문 실적 | 2026-08-07 SCHD 2건 ($2 · $10), 즉시 전량 체결, 수수료 $0 |
| 목적 | 수익이 아니라 **체결 비용 실측** — 슬리피지와 환전이 가정한 10bp 안에 드는가 |

구현된 것:

- 토스증권 Open API 인증, 계좌·보유 종목·시세 조회
- 포트폴리오 비중 및 집중도(HHI) 분석
- 미국 ETF 기반 PAPER Trading 엔진
- 거시(FRED)·감성·추세·MACD 통합 시장 점수와 연속 목표 비중
- 변동성 기반 익스포저 조절
- 대칭 리밸런싱 — 부족분 매수와 초과분 부분매도
- 무거래 밴드 + 목표 이탈 상한(drift cap)
- 손절 정책(추적 청산·최대 보유기간은 기본값 꺼짐)
- 일일·누적 손실 한도 **경고**(매매는 중단하지 않음)
- 거래비용 모델과 고정 벤치마크 둘
- 사이클별 append-only 의사결정 이벤트 로그
- 실주문 실행 계층 — 브로커 계약, 덧붙이기 전용 원장, 결과 분류, 슬리피지 계측,
  호출 제한, 긴급 중지
- 실데이터 백테스터 — 운영 코드를 그대로 재생, FRED 개정 이력으로 미래참조 차단
- Telegram 일일 보고서와 systemd 자동 실행
- Node.js Test Runner 기반 자동 테스트 29개 파일

---

# 💡 핵심 기능

## 1️⃣ Toss API 포트폴리오 조회

- OAuth2 인증과 프로세스 내 토큰 재사용
- 국내·미국 계좌와 보유 종목 조회
- 환율, ETF 현재가·호가 조회
- 금액·수량·수익률은 API 원본처럼 **문자열로 유지**해 정밀도 손실을 피합니다

## 2️⃣ 포트폴리오 분석

- 통화별 평가액 분리(원화와 달러를 더하지 않습니다)
- 종목 비중과 집중도 지수(HHI)
- 단일 종목 비중 40% 이상 경고
- PAPER 장부를 건드리지 않는 **읽기 전용**

## 3️⃣ 통합 시장 분석

시장 상태는 셋입니다 — **RISK_ON / NEUTRAL / RISK_OFF**

### 통합 점수 계산

```text
합산점수 = 거시 점수 × MACRO_SCORE_WEIGHT
        + 감성 점수 × 감성 신뢰도 × SENTIMENT_SCORE_WEIGHT × 신선도
        + 추세 점수 × 추세 신뢰도 × TREND_SCORE_WEIGHT
        + MACD 점수 × MACD 신뢰도 × MACD_SCORE_WEIGHT
```

합산점수는 아래 표 **사이를 연속적으로 보간**해 목표 비중이 됩니다.
±1.5에서 계단처럼 튀지 않습니다.

| 시장 상태 | VTI | SCHD | IWM | 현금 |
|---|---:|---:|---:|---:|
| RISK_ON | 70% | 15% | 15% | 0% |
| NEUTRAL | 70% | 20% | 0% | 10% |
| RISK_OFF | 40% | 20% | 0% | 40% |

### 현재 가중치 — 배분을 움직이는 층은 하나뿐입니다

| 층 | 가중치 | 이유 |
|---|---:|---|
| 거시(FRED) | **0** | 상수인 동안 신호가 아니라 노출 다이얼이었고, 값이 움직이는 것은 평균적으로 해로웠습니다(2008 한 구간만 우위) |
| 감성 | **0** | **보류이지 결론이 아닙니다.** 미검증 층이 노출을 6.6%p 깎고 있었고, 백테스터는 감성을 넣지 않아 실운영이 잰 적 없는 스택이었습니다 |
| 추세 | **1** | 0.5보다 낙폭이 낮고 2보다 Sharpe가 높습니다(양쪽 4/4) |
| MACD | **0** | 기여 상한이 무거래 밴드보다 작아 주문을 만들 수 없습니다 |

**가중치가 0인 층도 점수는 계속 계산되고 이벤트 로그에 남습니다.** 배분에만
관여하지 않으므로, 표본이 쌓이면 언제든 다시 판정할 수 있습니다.

### FRED 거시경제 분석

미국 기준금리와 90일 변화, 근원 PCE 수준과 변화, 실업률 변화, Sahm 경기침체
지표, 10년물·2년물 금리차를 봅니다.

백테스트에서는 **FRED 개정 이력(vintage)** 으로 "그날 알 수 있었던 값"만 씁니다.
지금 조회한 확정치를 넣으면 미래를 보고 매매한 것이 되는데, **결과가 좋게 나오기
때문에 눈에 띄지 않는** 종류의 오류입니다.

### 공식 뉴스와 공개 의견 감성

수집 대상: Federal Reserve 공식 RSS, GDELT 경제뉴스, Google News RSS, 선택한
공개 Bluesky 계정, Substack·개인 블로그 RSS. 모두 API 키가 필요 없습니다.

- 공식 보도와 의견 콘텐츠를 분리해 분석하고, 의견은 기본 10%만 보조 반영
- **소스별 성공·실패를 스냅샷마다 기록**합니다. 죽은 소스가 조용히 표본을
  줄이는 일을 막기 위해서입니다
- 기본 분석기는 프로젝트 내부 규칙 기반 로컬 분석기라 외부 LLM 비용이 0원이며,
  필요하면 Ollama 로컬 모델을 쓸 수 있습니다

### Faber 200일 이동평균 추세

- 일봉: Twelve Data(키) → Yahoo Finance → Stooq 순 폴백
- 200일 이동평균 대비 거리를 `tanh`로 연속 점수화
- 여러 ETF의 데이터 준비 비율과 방향 일치도를 신뢰도로 반영
- 약 20시간 캐시와 stale 폴백

**현재 배분을 움직이는 유일한 층**입니다.

### MACD 보조 신호

- 캐시된 **일봉 종가**로 12·26·9 계산 (종목당 최소 34개 표본)
- 기본 가중치 `0` — 계산과 표시는 하되 판단에는 쓰지 않습니다

## 4️⃣ 변동성 기반 익스포저 관리

```text
익스포저 배수 = clamp(목표 변동성 ÷ 실현 변동성, 최소 익스포저, MAX_EXPOSURE)
```

- 최근 20거래일 수익률로 연율 변동성 추정
- 목표 연율 변동성 **0.15**
- 축소 하한은 기본 익스포저의 30%, 상한은 **1** (1.2로 올리면 4개 구간 전부 악화)
- 줄어든 주식 비중은 현금으로 이동

Moreira & Muir(2017)의 변동성 관리 아이디어를 단순화한 것이며,
**측정 결과 이 전략이 가진 우위의 84~99%가 여기서 나옵니다.**

## 5️⃣ PAPER Trading 시스템

파일 기반 가상 장부로 ETF 매매 전략을 검증합니다.

```text
초기 원금:                   100,000원 (코드 수준 하드캡)
1회 최대 매수:              $5
최소 주문금액:               $1
하루 최대 매수:             $10
무거래 밴드:                목표 비중 대비 5%
목표 이탈 상한:             2배 (TARGET_DRIFT_CAP)
청산 후 재진입 대기시간:    24시간
편도 거래비용률:            0.1%
목표 연율 변동성:           0.15
```

- 실제 계좌와 PAPER 장부를 분리하고, 이전 장부를 이어서 사용
- 임시 파일 + rename의 원자적 상태 저장, 잠금 파일로 중복 실행 방지
- `$1` 미만의 작은 차이는 주문하지 않습니다 — `$0.02`짜리 주문이 15분마다
  반복되던 문제를 막았습니다

## 6️⃣ 대칭 리밸런싱과 이탈 상한

- 목표보다 부족한 ETF는 한도 안에서 매수
- 목표를 5% 밴드 이상 초과한 ETF는 목표선까지 부분매도
- 손실 브레이크로 신규 매수가 멈춰도 **위험 축소 매도는 계속** 실행
- 리밸런싱 매도에는 매수용 상한을 적용하지 않음 — 나가는 속도와 돌아오는 속도를
  맞추기 위해서입니다
- Agent가 PAPER 장부에서 만든 포지션만 대상
- 부분매도 시 원가와 실현손익을 비례 정산

무거래 밴드에는 **방향 비대칭**이 있습니다. 자산이 커지면 밴드도 커져서, 한 번
벌어진 이탈이 되돌려지지 않고 굳는 경우가 생깁니다. 해법으로 **목표 이탈
상한**(`TARGET_DRIFT_CAP=2`)을 두었습니다 — 이탈이 밴드의 2배를 넘으면 밴드와
무관하게 되돌립니다. 다만 이것이 문제의 절반만 덮는다는 것도 측정돼 있습니다.

## 7️⃣ 리스크 관리 및 청산 정책

Agent가 PAPER 장부에서 직접 만든 포지션만 자동 청산 대상입니다. Agent 실행 전부터
실제 계좌에 있던 종목은 자동 매도에서 제외됩니다.

### 손절 청산

- 기본 **12%**. 매매 규칙이 아니라 **재난 방어선**입니다
- 측정 결과 거의 발동하지 않으며 폭락 방어에 기여하지 않습니다. 값을 바꿔도
  결과가 사실상 같으므로, 어떤 값이든 "보험료"로만 봅니다

### 추적 수익 청산 · 최대 보유기간 청산 — **기본값 꺼짐**

개별 종목용 모멘텀 규칙이라 지수 ETF에서는 배분 레이어와 충돌합니다. 배분이
"VTI 70% 보유"라고 말하는 동안 청산 레이어가 지수의 일상적 등락에 걸려 포지션을
통째로 비웠고, 20년 실데이터에서 **회전율만 약 21배**로 키우고 수익은 늘리지
못했습니다.

### 손실 한도 — **중단이 아니라 경고입니다**

- 기본 일일 `$3`, 누적 `$10`
- **한도에 닿아도 매매 동작은 전혀 바뀌지 않습니다.** 경고를 기록하고 리포트에
  표시할 뿐입니다
- 자동 중단은 20년 실데이터에서 최대 낙폭을 **29.8% → 51.6%로 키웠습니다.**
  매수는 멈추는데 방어 매도는 계속되니 전액 현금이 되고 다시 들어가지 못해,
  남은 17년 동안 자산이 얼어붙었습니다
- 손실에 어떻게 대응할지는 경고를 본 **사람**이 판단합니다

## 8️⃣ 거래비용 모델

- 기본 편도 비용률 **0.1% (10bp)**
- 증권 수수료, FX 스프레드, bid/ask, 슬리피지를 합친 단일 대리값
- 누적 비용을 `feesUsd`로 기록하고 Telegram 보고서에 표시
- 벤치마크에도 동일한 진입 비용 적용

**실측(2026-08-07, 실계좌 2건): 수수료는 현재 `$0`입니다.** $10 주문에서도 0이므로
0.1%가 아닙니다. 그럼에도 `TRADE_COST_RATE`를 낮추지 않습니다 — 이 값은 수수료만이
아니라 **슬리피지와 환전까지 덮는 가정**이고, 그 둘은 아직 재지 않았습니다.
(토스 `/commissions`의 US 요율 면제 종료일이 2026-08-08이라 곧 10bp가 붙을
가능성도 있습니다. 그러면 우리 가정과 정확히 일치합니다.)

## 9️⃣ 고정 벤치마크 둘

절대 손익만으로는 전략이 단순 보유보다 나은지 알 수 없습니다.

- **VTI 매수후보유** — 원금을 한 번 넣고 그대로 둔 경우
- **정책믹스 고정** (VTI 70 / SCHD 20 / 현금 10, 리밸런싱 없음) — **위험을 맞춘**
  기준선이라, 이것과의 차이가 신호 층 자체의 성과입니다

두 기준선 모두 **최초에 고정되고 전략의 행동에 반응하지 않습니다.**

alpha는 다시 둘로 쪼갭니다.

```text
설계된 비용 = (목표 주식 비중 − 1) × 벤치마크 수익률   ← 일부러 현금을 든 값
결함       = (실제 − 목표)     × 벤치마크 수익률   ← 목표에 못 간 것
```

첫째는 위험 결정이고 둘째는 버그입니다. **대응이 정반대**라 반드시 나눠 봅니다.

## 🔟 Append-only 이벤트 로그

PAPER 사이클마다 `paper-events.jsonl`에 한 줄씩 추가합니다.

- 실행 시각, 시장 상태와 합산 점수
- **가중치를 적용하기 전의 층별 원점수**와 가중치, 그 시점 가격
  (곱해진 기여도만으로는 나중에 되돌릴 수 없기 때문입니다)
- 목표 비중, 변동성과 익스포저 배수
- 매수·매도·중단 결정과 이유
- 총자산, 현금, ETF 평가액, 실현·미실현·총손익, 누적 거래비용
- 벤치마크와 alpha

```bash
npm run paper:events -- --limit=50
```

## 1️⃣1️⃣ Telegram 일일 보고서

뉴욕 정규장 종료 10분 후 PAPER 결과를 정리해 전송합니다. 같은 거래일의 보고서가
중복 전송되지 않도록 마지막 전송 거래일을 별도 상태 파일에 저장합니다.

보고 항목: 거래일, 초기 원금과 현재 총자산, 현금·ETF 평가액, 누적 손익과 수익률,
실현·미실현 손익, 누적 거래비용, 벤치마크와 alpha, 누적 거래 횟수, 통합 시장
상태와 점수, 층별 점수와 준비 상태, 연율 변동성과 익스포저 배수, ETF별 목표
비중, 보유 현황, 당일 매매 내역, 안전 중단 여부, PAPER 모드 안내.

## 1️⃣2️⃣ 실주문 실행 계층

**주문이 나가는 유일한 경로는 사람이 직접 실행하는 CLI입니다.**

```bash
npm run live:probe                       # 확인만 — 주문 안 나감
npm run live:probe -- --symbol SCHD --amount 2 --confirm
npm run live:slippage                    # 실제 체결의 슬리피지
npm run stop                             # 긴급 중지
```

설계 원칙은 하나입니다 — **실패하면 멈춘다. 추측하지 않는다.** 추측의 실패
모드가 진짜 돈으로 하는 중복 매수이기 때문입니다.

- **브로커 계약을 목록으로 판정합니다.** 새 브로커 API를 받았을 때 안전하게
  돌릴 수 있는지를 눈대중이 아니라 체크리스트로 정합니다. 가짜 브로커와 실제
  어댑터가 **같은 계약 테스트**를 통과합니다
- **원장은 덧붙이기 전용**이고 상태는 저장하지 않고 매번 로그에서 다시
  계산합니다. 저장된 상태와 로그가 어긋날 수 없습니다
- **미결 주문이 하나라도 있으면 다음 주문을 내지 않습니다**
- **모르는 주문 상태값은 멈추고 사람을 부릅니다.** 토스가 "알 수 없는 상태값도
  허용하라"고 명시하므로, 임의로 접어 넣으면 조용히 틀린 판단을 합니다
- **재제출 가능 여부는 한 곳에서만 정합니다.** 호출부마다 판단하면 언젠가 한 곳이
  "실패했으니 다시 내자"로 처리하고, 그것이 중복 매수가 됩니다
- **대사 대상은 "기준선 + 우리가 체결시킨 것"입니다.** 계좌에 이미 다른 자산이
  있어서(2026-08-07 확인) 계좌 전체로 대사하면 영구 정지합니다
- **긴급 중지는 환경변수가 아니라 파일**입니다. 멈추고 싶은 순간은 대개 뭔가
  잘못 돌고 있는 순간인데, 재시작을 요구하면 스위치가 늦게 듣습니다

## 1️⃣3️⃣ 백테스터

파라미터는 **백테스트에서** 정합니다. PAPER 20거래일 × $67로는 경로 의존 규칙의
효과를 잡음과 구분할 수 없습니다.

- **운영 코드를 그대로 재생합니다** — 백테스트용 사본을 만들지 않습니다
- 합성 4국면(상승·하락·횡보·모멘텀) + 20년 실데이터
- 정적 배분 대조군을 같은 엔진·비용·밴드로 함께 돌립니다. 신호 층이 값을 하는지
  아는 유일한 방법입니다
- 거시 층은 FRED 개정 이력으로 되살립니다
- **뉴스 감성은 넣지 않습니다.** 가격·추세·MACD·FRED는 다시 받을 수 있지만
  뉴스 감성 창은 되살릴 수 없어서, 실운영 로그가 이 층을 언젠가 백테스트할
  유일한 길입니다

---

# 📊 지금까지 측정한 것

## 20년 실데이터 (2006~2026)

```text
CAGR:        5.99%
MDD:         29.9%
평균 노출:   75.6%
```

현재 실운영 스택(거시 0 · 감성 0 · 추세 1 · MACD 0 · 변동성목표 0.15)은
**백테스터가 잰 스택과 정확히 같습니다.** 그래서 PAPER 성과를 위 숫자와 직접
비교할 수 있습니다.

## 확정된 결론

| 무엇 | 결론 |
|---|---|
| 신호 스택 vs 정적 배분 | **낙폭에서 이깁니다** — 노출 보정 후 10~16%p |
| 수익률 우위 | **없습니다.** 연 1.9~2.6%p를 낙폭 12~23%p와 바꿉니다 |
| 우위의 출처 | **변동성 관리**(전체의 84~99%). 추세는 대부분 중복이고 잔여 가치는 2008년 포함 표본에만 |
| 손절 | 거의 발동하지 않고 폭락 방어에 기여하지 않습니다 |
| 손실 한도 자동 중단 | 결과를 **악화**시킵니다 (MDD 29.8% → 51.6%) |
| 익스포저 상한 1.2 | CAGR·Sharpe·MDD·CDaR 전부 4/4 악화 — **기각** |
| 거시 층 | 상수인 동안 노출 다이얼일 뿐이고, 움직이면 평균적으로 해롭습니다 — 배분에서 제외 |

> **한 줄로: 이 시스템은 시장보다 더 버는 장치가 아니라 덜 잃는 장치입니다.**
> 의도된 거래입니다.

## 남은 판정 하나

**감성 층이 신호인지 잡음인지.** 8/20에 10거래일이 차서 처음 판정했고 결과는
**애매**였습니다 — 일별 자기상관 0.140으로 판정표의 가운데 칸입니다. 그 자리에서
이 검정의 해상도를 처음 쟀는데, 표본 10의 표준오차가 **0.32**라 판정표 세 칸을
전부 덮었습니다. **나온 "애매"는 감성 신호의 성질이 아니라 표본 10개의 성질입니다.**

그래서 **60거래일(2026-10-30)에 한 번 더 판정하고 끝냅니다.** 문턱(0.1/0.3)은
그대로 두고, 같은 자리에서 **종료 조건**을 함께 박았습니다 — 그날 또 0.1~0.3이면
연장 없이 감성 가중치 0으로 확정합니다. 판정 불가는 기본값이 이깁니다.
상세는 `STRATEGY.md` §3 ②.

## 주요 이정표

경위와 근거는 각 로그에 있습니다. 아래는 색인입니다.

| 날짜 | 무엇 | 로그 |
|---|---|---|
| 07-18 | 초기 구축과 신호 결합 | [`2026-07-18`](develope-log/2026-07-18_초기구축과-신호결합.md) |
| 07-25 | 대칭 리밸런싱과 검증 구조 | [`2026-07-25`](develope-log/2026-07-25_대칭리밸런싱과-검증구조.md) |
| 08-06 | 사후분석과 백테스터 — alpha의 80%가 결함이었음 | [`2026-08-06`](develope-log/2026-08-06_사후분석과-백테스터.md) |
| 08-06 | 변동성 목표 0.15 확정, 손절 문턱 확정, 추세 가중치 확정 | [`변동성`](develope-log/2026-08-06_변동성목표-확정.md) · [`손절`](develope-log/2026-08-06_손절문턱-확정.md) · [`추세`](develope-log/2026-08-06_추세가중치-확정.md) |
| 08-07 | 밴드 폭 0.05 확정과 방향 비대칭, 드리프트 캡 채택 | [`밴드`](develope-log/2026-08-07_밴드폭-확정과-방향비대칭.md) · [`캡`](develope-log/2026-08-07_드리프트캡-채택과-교차점-정정.md) |
| 08-07 | 익스포저 상한 기각, 거시 층 검증 | [`익스포저`](develope-log/2026-08-07_익스포저상한-기각.md) · [`거시`](develope-log/2026-08-07_거시층-검증.md) |
| 08-07 | 실주문 실행 계층 구축 | [`실행계층`](develope-log/2026-08-07_실주문-실행계층.md) |
| 08-07 | **첫 실주문 2건** — $12로 영구 정지 버그를 찾음 | [`첫 실주문`](develope-log/2026-08-07_첫-실주문.md) |
| 08-07 | 슬리피지 계측 계층 | [`슬리피지`](develope-log/2026-08-07_슬리피지-계측.md) |
| 08-08 | 감성 소스 복구 — 세 번 틀린 진단 | [`감성`](develope-log/2026-08-08_감성-소스-복구.md) |
| 08-19 | 판정 표본이 리셋되던 것 — 구성과 가용성을 가름 | [`지문`](develope-log/2026-08-19_판정표본-지문규칙-정정.md) |
| 08-21 | 첫 감성 판정은 애매 — 표본 60거래일과 종료 조건 | [`해상도`](develope-log/2026-08-21_감성판정-해상도와-종료조건.md) |
| 08-21 | 문서가 0이라고 적은 감성 가중치가 서버에서는 1이었음 | [`가중치`](develope-log/2026-08-21_감성가중치가-서버에서만-1이었다.md) |

---

# 🚧 다음 단계

## 지금 진행 중

- **2026-09-01 실제 돈 전환** — 원금 10만 원(코드 상한), 1회 주문 $5 · 일일 매수 $10 · 총 손실 한도 $10
- **감성 층 판정** — 8/20 첫 판정은 애매(자기상관 0.140). 60거래일 표본으로 2026-10-30에 확정
- 11월 PAPER 병행 실운용으로 **체결 비용 실측**(슬리피지·환전이 10bp 안인가)
- Telegram 기반 실사용 테스트와 보고서 가독성

## 그다음

- 실주문 실행기를 예약 실행에 연결할지 판단 (지금은 사람 손으로만)
- 무거래 밴드 방향 비대칭의 나머지 절반
- Telegram 대화형 명령과 투자 판단 근거 자동 요약
- 이벤트 로그 기반 분석 대시보드와 시각화
- 상태 파일 스키마 버전과 마이그레이션
- 독립적인 기술·투자·리스크 검토

---

# 🛠 기술 스택

## Runtime

Node.js 20.12 이상 · JavaScript ES Modules · **외부 런타임 패키지 없음** ·
Node.js 내장 Test Runner

## Data & API

Toss Securities Open API · FRED API(개정 이력 포함) · Telegram Bot API ·
Federal Reserve RSS · GDELT DOC API · Google News RSS · Bluesky Public API ·
RSS/Atom · Twelve Data 일봉(폴백: Yahoo Finance, Stooq) · 선택형 Ollama Local API

## Investment Engine

PAPER Trading Engine · Portfolio Analysis · Macro Regime Analysis(vintage 기반
되살리기) · Local News Sentiment Analysis · Faber 200-day Trend Signal ·
Volatility-managed Exposure · MACD Confirmation Signal · Integrated Market Signal ·
Symmetric Rebalancing + Drift Cap · Exit Strategy Engine · Transaction Cost Model ·
Fixed Dual Benchmark · Alpha Attribution · Append-only Event Log

## Live Execution

Broker Contract · Toss Order Adapter · Append-only Order Ledger ·
Order Lifecycle / Outcome Classification · Position Baseline Reconciliation ·
Slippage Measurement · Rate Limiter · File-based Emergency Stop · Fake Broker

## Infrastructure

Oracle Cloud Linux · systemd Service/Timer · 파일 기반 장부 · JSONL 이벤트 로그 ·
원자적 상태 저장 · 프로세스 중복 실행 방지

---

# 🧠 프로젝트에서 중요하게 생각한 점

TAPIoca는 단순히 주식을 자동으로 사고파는 프로그램이 아닙니다.

> **"AI 투자 에이전트에게 실제 자산을 맡기기 전에 무엇을 검증해야 하는가?"**

이 질문에 답하기 위한 실험입니다.

설계 원칙:

- **결과를 보기 전에 판정 기준을 적는다.** 보고 나서 정하면 원하는 결론이 나옵니다
- 미검증 층은 배분에서 **뺀다.** 신호인지 잡음인지 모르는 것이 노출을 바꾸고 있으면
  안 됩니다
- 실운영 스택과 **백테스트한 스택을 일치시킨다.** 다르면 잰 적 없는 것을 돌리는
  것입니다
- 파라미터는 백테스트에서 정하고, **구간을 나눠 부호 일치를 본다.** 전 구간
  평균 하나로는 노이즈와 구분되지 않습니다
- 기준선은 **위험을 맞춰서** 세우고, 전략의 행동에 반응하지 않게 고정한다
- alpha를 **설계된 비용과 결함으로 나눈다.** 대응이 정반대입니다
- 실제 보유 자산과 Agent 포지션을 분리하고, 투자금과 최대 손실을 코드로 제한한다
- 하나의 신호만으로 즉시 매매하지 않는다
- 실패하면 **멈춘다. 추측하지 않는다** — 특히 실주문에서
- 모든 결정과 결과를 append-only 로그로 보존한다
- 자동 테스트로 안전장치의 **실제 동작**을 검증한다
- **가장 작게 시작해서 준비가 덜 된 곳을 찾는다.** "충분히 준비되면 시작한다"는
  순서는 틀렸습니다 — 테스트 314개를 통과하고도 남아 있던 영구 정지 버그가
  $2짜리 실주문 한 건에서 나왔습니다

---

# 🔒 안전장치

## 자동 실행 경로

- **예약 실행기는 PAPER 모드가 아니면 실행을 거부합니다.** 타이머가 주문을 낼 수
  있는 경로는 없습니다
- 미국 정규장 밖에서는 기본 실행 중단, 중복 프로세스 실행 방지
- 시장 데이터 확인 실패 시 신규 매수 중단
- 임시 파일과 rename을 이용한 원자적 장부 저장

## 실주문 경로 (사람이 직접)

- `--confirm` 없이는 **어떤 주문도 나가지 않습니다**
- `live:probe`는 1회 주문 금액에 상한이 있습니다
- 미결 주문이 있으면 다음 주문을 내지 않습니다
- 모르는 브로커 상태값은 멈추고 사람을 부릅니다
- 원장은 덧붙이기 전용이며 고치지 않습니다
- `data/EMERGENCY_STOP` 파일 하나로 재시작 없이 즉시 중지됩니다

## 자금과 포지션

- 투자 원금 100,000원 하드캡, 1회·하루 최대 매수금액 제한, 최소 주문금액 `$1`
- 실제 계좌와 PAPER 장부 분리, 대사는 기준선 + 우리 체결분에 대해서만
- 기존 보유 종목 자동 매도 금지
- 손실 한도 도달 시 경고(자동 중단은 낙폭을 키워 제거했습니다)
- 높은 변동성에서 주식 익스포저 축소, 청산 후 24시간 재진입 대기

투자 전략의 결과는 수익을 보장하지 않습니다. 현재 설정은 검증을 위한 실험값이며,
**실주문 실행기를 자동 실행에 연결하는 것은 아직 하지 않았습니다.**

---

# 📎 GitHub

<https://github.com/Real-Woong/TAPIoca.git>

---

# ✨ 한 줄 요약

**거시·뉴스 감성·200일 추세·MACD·변동성 관리를 결합해 미국 ETF 전략을 PAPER로
검증하고, 20년 실데이터로 각 층이 값을 하는지 따로 재고, 실계좌 소액 주문으로
체결 비용을 실측하는 개인 투자 에이전트 프로젝트 — 더 버는 장치가 아니라 덜 잃는
장치를 목표로 합니다.**
