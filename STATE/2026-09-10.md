# 2026-09-10 (거래일 뉴욕 2026-09-09)

LIVE 6거래일째. **9/9 관문(램프 종료 후 목표미달)을 읽었다 — 결함이 아니다.**

## 읽은 것

| 창 | 사이클 | 목표미달 | 구조적 | 초과 |
|---|---|---|---|---|
| **9/9 (판정)** | 26 | **−$0.0051** | +$0.0121 | −$0.0748 |
| 9/1~9/9 (램프 규칙상 판정제외 포함) | 177 | −$0.0114 | +$0.0284 | +$0.0938 |
| 7/27~ (도구 기본값) | 824 | −$1.46 | +$0.04 | alpha −$1.13 |

**26 사이클은 정확히 한 세션이다**(`CYCLES_PER_SESSION = 26`). 판정 구간에
빠진 사이클이 없다.

**램프 나흘(7/30·31·8/03·8/04)의 목표미달이 −1.62다.** ⑰에 적어 둔 값과 같다.
LIVE 6거래일 전체가 −0.0114이므로 **142배 차이**다. ⑰의 "그 나흘은 램프였다"가
이것으로 확인됐다 — 램프가 끝나자 항목이 사라졌다.

## 부호는 음수인데 왜 결함이 아닌가

**9/9의 비중 갭(실제 주식비중 − 목표)이 26/26 전부 양수다.**

```
스텝 26 | 최소 0.0157 · 최대 0.0167
목표보다 0.1%p 이상 미달인 사이클: 0 / 26
```

**하루 내내 목표보다 +1.6%p 더 들고 있었다.** 못 채운 구간이 한 번도 없다.
체결 경로 결함은 정의상 **실제 < 목표**로 나타나는데 부호가 반대다.

**그러면 목표미달 항목은 왜 음수인가.** 이 항목은 `(실제−목표) × 벤치수익률`
이고, 9/9는 벤치마크가 **내린** 날이다(구조적 +0.0121이 그것을 말한다 —
`(목표−1)`이 음수인데 곱이 양수다). 목표보다 더 들고 있는데 시장이 내렸으니
곱이 음수다. **덜 사서 뒤처진 것이 아니라 더 들고 있어서 더 내린 것이다.**

**+1.6%p는 밴드(5%) 안이라 주문이 안 나간다.** 9/4에 적은 상태 그대로다 —
가장 가까운 SCHD가 밴드까지 $0.77 남아 있었다. **조용한 것은 고장이 아니다.**

## 판정

**관문 4(램프 종료 후 목표미달 ≈ 0)는 섰다.** 다만 **표본이 1거래일이다.**
관문 다섯 중 마지막은 어차피 3(대사, 11/25)이므로 **오늘로 닫지 않고
11/25 취합 때 같은 창으로 다시 읽는다.** 표본 부족은 표본 부족으로만 말한다.

## 규칙 문구 둘이 갈렸다 — ㉕

§3은 "**음수로 남아 있으면** 진짜 체결 경로 결함", 증액 관문 4는
"**목표미달 ≈ 0**"이라고 적혀 있다. −0.0051은 앞엣것으로 결함, 뒤엣것으로 통과다.
**숫자를 본 뒤에 유리한 쪽을 고르면 그게 §3이 막으려던 짓이다.**

그래서 문구를 고르지 않고 **부호가 아닌 비중 갭으로 읽었다.** 그 검사는 시장
방향과 무관하고, 애초에 §3이 물으려던 것("못 채우고 있는가")에 직접 답한다.
상세와 남은 결정은 `STRATEGY.md` ㉕.

## 도구가 이 질문에 답을 못 한다 — 그래도 9월엔 안 고친다

`npm run paper:alpha`는 `PAPER_DATA_DIR` 전체를 읽고 **창을 자를 옵션이 없다**
(`alpha-cli.js`). 그래서 오늘 그냥 돌리면 7/27~ 누적을 내고, 램프에 눌려
**"목표 미달이 더 큽니다 → 고칠 대상입니다"를 그대로 다시 낸다.** ⑰이 오진이라고
판정한 그 문장이다.

**9월 동결이므로 코드는 안 건드렸다.** 기존 `alpha-attribution.js`를 그대로
부르는 일회성 스크립트로 창만 잘라 읽었다. `--from` 옵션은 10월 이후 후보다.

## 곁에 둘 사실 — 출력이 셸에 먹혔다

터미널 출력을 복사해 옮기는 사이에 `$`+숫자가 위치 파라미터로 전개됐다.
zsh에서 `$0`은 셸 이름, `$1`~`$9`는 빈 문자열이다.

```
-$1.46  →  -.46      (Ⓐ $1이 사라짐)
+$0.04  →  +-zsh.04  (Ⓑ $0이 셸 이름으로)
```

**숫자가 조용히 바뀐다.** `-.46`은 깨진 것처럼 안 보이고 −$0.46으로 읽힌다.
복원은 항목 합으로 검산했다(0.04 − 1.46 + 0.49 − 0.20 = −1.13 = alpha).

**옮길 때는 파일을 거친다** — `npm run paper:alpha > /tmp/a.txt`.
`usd()`가 `$`를 안 쓰게 고치는 것은 출력 형식 변경이라 9월엔 안 한다.

## 곁들여 확인한 것 — 대사 5/60, 무중단

**세는 첫날은 9/2다** — 9/1은 ⑳으로 멈춘 날이라 뺀다.

```
9/2 · 9/3 · 9/4 · [9/7 노동절 휴장] · 9/8 · 9/9  →  5거래일
```

남은 **55거래일이 11/25(수)**다. 9/2부터 9월이 20거래일이라는 계산과 맞는다.

**카운터는 코드에 없다**(`grep -rn "streak\|consecutive" src/` 0건). 중단은
`execution-plan.js`의 halt로만 남으므로 저널로 센다.

```bash
journalctl -u toss-ai-paper --since "2026-09-02" | grep -c "어긋납니다"   # → 0
```

**0건을 그대로 믿지 않고 양성 대조군을 먼저 봤다.** 없는 것을 봤다고 말하려면
있을 때 보이는지를 먼저 봐야 한다 — ⑬에서 픽스처가 운영이 안 만드는 상태를
만들어 검사를 덮고 있었던 것과 같은 자리다.

```bash
journalctl -u toss-ai-paper --since "2026-09-01" --until "2026-09-02" | grep "어긋납니다"
Sep 01 14:05:21 marvis-bot node[285123]:   멈춤(RECONCILE_MISMATCH): 브로커와 장부가 어긋납니다: SCHD -0.356983
```

**패턴이 실제 중단을 잡는다.** 값(−0.356983)이 ⑳에 적힌 것과 같고, 시각도 알파
창의 첫 스텝(`2026-09-01T14:05:20.737Z`)과 같은 순간이다. 그래서 9/2~9/9의
0건이 비로소 근거가 된다.

**로그가 그 구간에 연속이다** — 8734줄, `■ 스택` 755건. 하루 96사이클(15분
주기) × 8일 ≈ 768이라 빠진 날이 없다.

> **11/25에도 같은 절차다.** 다만 **양성 대조군이 9/1 하나뿐이고 저널이 밀리면
> 같이 사라진다.** 그때는 `data/` 이벤트 로그의 halt 기록으로 가야 한다.

## 안 한 것

`DEVELOPE_IDEA/` 001·003은 그대로 10월 이후다. 오늘 코드 변경 0건.
