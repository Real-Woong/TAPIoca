# 2026-09-04 (거래일 뉴욕 2026-09-03)

LIVE 3거래일째. 폴더 셋의 분업을 이 날부터 시작한다(`STATE/README.md`).

## 맞는 것

| 확인 | 값 |
|---|---|
| 자산 정합 | $66.65 + $1.50 = $68.15 · 종목 손익 합 +$1.35 · 실현 −0.25 + 미실현 +1.35 = +1.10 |
| 기준선 앵커 | $2.33 / **66.71** = 3.493% · $0.58 / **67.63** = 0.858% — ㉓의 재조정이 그대로 살아 있다 |
| 주문 0건 | 이탈이 밴드($68.15 × 0.05 = **$3.41**) 안이다. SCHD +$2.5 초과 · IWM −$1.7 미달 |
| 미결 0건 | `⚠️ 결말이 안 난 주문` 줄이 없다 = `unresolvedCount 0`. ㉒ 이후 매매가 안 묶였다 |
| GDELT 429 | 기존 상태다(⑪ — 10거래일 중 9일). 요청 구성이 같으므로 표본을 가르지 않는다 |

## 걸린 것

**㉔ `실행 스택` 줄이 생긴 날부터 한 번도 안 찍혔다.** `통합 시장 상태` 다음이
곧바로 `신호 기여`였다. `formatStackLines`는 `state.macro.weights`만 읽는데
운영에서 그 상태를 만드는 `compactMacroSignal`이 화이트리스트라 `weights`를
안 실었다. 08-21에 그 줄을 만들면서(⑬의 수정) 같이 안 바뀌었고, 그 뒤 14일치
보고서가 전부 그랬다.

**빠진 것은 정보가 아니라 경보다** — 감성 가중치 ≠ 0이면 함께 다는
`⚠️ 미검증 층 작동 중`이 ⑬이 다시 나도 침묵한다. 고쳤다(`a9c367d`).
상세는 `STRATEGY.md` ㉔.

**내일 아침 그 줄이 `감성 0`인지 본다.** 9/1~9/3에 실제로 돈 스택은 이 줄로는
확인되지 않는다 — 확인 경로는 `paper:run` 배너의 `■ 스택:`이고 그것은 멀쩡하다.

## 확인할 것

**대사 60거래일(관문 3)의 도달일이 문서와 안 맞는 것 같다.** 대사는
`runLiveCycle` 2단계에 있어 **9/1이 첫날**인데, 9/1부터 60거래일을 세면
9월 21 · 10월 22 · 11월 17로 **11월 24일(화)**이다. `STRATEGY.md`는 관문
다섯이 다 설 수 있는 가장 이른 시점을 **11월 초**로 적어 뒀다. 세는 시작점이
다른 것인지 확인이 필요하다. **증액 시점의 문제라 급하지 않다.**

## 안 한 것

**폴더 셋을 만들었고 아이디어 셋을 적었다** — 001 보고서 자기점검 ·
002 LLM 일일 피드백 · 003 환전 비용 실측. **셋 다 지금 하지 않는다.**
9/9(램프 종료 후 목표미달)와 10/30(감성 판정)이 걸려 있어, 보고서 산출이나
관측 대상을 건드리는 변경은 그 판정이 읽는 것을 바꾼다.

## 미결 — 9월 이후를 어떻게 할 것인가 (결정 전)

"실거래를 9월까지만 하고 멈출까"가 나왔다. **결정하지 않았다.** 기준을
`STRATEGY.md`에 옮기는 것은 사람이 정한 뒤다 — 여기 적는 것은 논의된 사실뿐이다.

- 관문 다섯 중 **LIVE만이 채울 수 있는 것은 1·2·3**(슬리피지 · 환전 · 대사)이다.
  4(목표미달)와 5(감성)는 PAPER 경로가 채우므로 LIVE와 무관하게 계속된다.
- 그중 **2(환전)는 시간이 아니라 한 번의 행동이 닫는다**(003).
- 즉 "9월까지"는 1·3을 열어 두고 닫는 길이다.
