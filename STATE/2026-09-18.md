# 2026-09-18 (거래일 뉴욕 2026-09-17)

**㉘ 첫 동기화 판독이다.** LIVE 13거래일 만의 첫 실주문이고, 4건 다 체결됐다.
`develope-log/2026-09-17_...` 「서버에서 볼 것」 셋을 순서대로 봤다.

## 맞는 것 — 장부 정합

| 확인 | 값 |
|---|---|
| 자산 정합 | 현금 1.50 + ETF 65.32 = **66.82** |
| 평가액 | 46.74 + 13.64 + 4.94 = **65.32** |
| 미실현 | +0.21 +0.08 −0.27 = **+0.02** |
| 누적손익 | 실현 −0.25 + 미실현 +0.02 = **−0.23** |
| 원금 대비 | 67.05 − 0.23 = **66.82** |
| 목표 비중 합 | 70 + 17 + 8 + 5 = **100** |
| IWM 목표 | 0.1 × 추세 0.826 = 8.26 → **8%** |

## 셋 다 봤다

**① SCHD 매도 $2.48이 먼저 나갔다.** 「오늘의 실주문」 첫 줄이 `SELL SCHD 요청
$2.49 → 체결 $2.48`이고, 나머지 셋은 전부 VTI 매수다. 매도 먼저·매수는 차이
큰 순이라는 순서가 운영에서 그대로 나왔다.

**② 실계좌 금액 매도가 통과했다 — 처음이다.** `[FILLED]`이고 `거절:` 줄이 없다.
보유의 98%까지만 내는 규칙이 실제 거절을 막았는지는 이 한 건으로는 모른다
(오늘 매도는 보유 0.475734 중 0.073300주라 상한 근처가 아니었다).

**③ 차이가 하루치만큼 줄었다.** 9/16 보고서 $44.77(64.68−19.91) → 오늘
**$31.87**. 줄어든 $12.90은 실계좌 순매수 $13.39에서 장부가 시세로 오른 몫을
뺀 것이다.

## 실계좌 수량이 8월 잔해와 이어붙는다

| 종목 | 어제까지 | 오늘 체결 | 오늘 |
|---|---|---|---|
| VTI | 0.005248 (8/28 probe) | +0.042437 (매수 3건) | **0.047685** |
| SCHD | 0.475734 | −0.073300 | **0.402434** |
| IWM | 0.006665 (8/28 probe) | 0 | **0.006665** |

**수량이 여섯째 자리까지 맞는다.** 대사가 `기준선 + 우리 체결 = 브로커`를 보고
있으므로, 주문이 나간 것 자체가 오늘도 대사가 맞았다는 뜻이다(안 맞으면
`decide`를 안 부른다).

## 한도 — 순매수 $13.39로 상한 안

체결 매수 6.69 + 6.69 + 2.49 = **$15.87**, 체결 매도 **$2.48** → 순매수
**$13.39 ≤ $13.4**. 오늘 판 만큼 더 살 수 있다는 규칙(`redeployableUsd`와 같은
규칙)이 그대로 돌았다.

> **요청액 합은 $15.90으로 방($15.88)보다 2센트 많다.** 체결이 끝난 매수를
> 요청액이 아니라 체결액(6.70 → 6.69)으로 세기 때문이고, 넘는 것은 요청뿐
> 실제 지출은 안 넘는다. 하루에 쌓이는 여유분은 체결 건당 센트 단위다.

## 남은 차이와 끝나는 날

| 종목 | 장부 | 실계좌 | 차이 |
|---|---|---|---|
| VTI | 46.74 | 17.90 | **28.84** |
| SCHD | 13.64 | 13.64 | **0.00 — 수렴** |
| IWM | 4.94 | 1.90 | **3.04** |

매도가 끝났으므로 앞으로는 하루 $13.4씩이다. 9/18(금) → $18.5, 9/21(월) →
$5.1, **9/22(화)에 VTI 잔여 $2.0과 IWM $3.0이 같은 날 들어가 0이 된다.**
경고 줄은 **9/23(수) 아침 보고서**에서 사라질 자리다. 시세와 밴드가 움직이면
하루쯤 밀린다.

**IWM은 맨 마지막이다.** 매수를 차이 큰 순으로 정렬하고 사이클당 1건만 내므로,
VTI 차이가 IWM보다 작아지기 전에는 IWM 차례가 안 온다. ㉘에 적은 "VTI $6.7 ·
IWM $3.01"은 한 사이클의 계획이지, 첫날에 둘 다 나간다는 뜻이 아니었다.

## 걸린 것 — 둘

**① 슬리피지 표본이 안 늘었다. 체결은 9건인데 측정 가능은 여전히 5건이다.**
`live:slippage`는 `PLANNED` 이벤트에 실린 `quote`(주문 직전 중간가)로만 잰다.
`live-probe.js`는 호가를 조회해 넣지만(175~198줄) **`live-cycle.js`의 `PLANNED`
기록에는 `quote`가 없다**(125~128줄) — 오늘 4건은 표에 줄로는 뜨되 중간가 칸이
비어 `측정 가능`에 안 들어간다.

**호가는 나중에 복원되지 않는다.** 그리고 동기화가 끝나는 9/22 이후에는 다시
연 2.8회 회전의 세계라, **표본을 모을 수 있는 창이 앞으로 3거래일뿐이다.**
관문 1(증액 조건 ①)과 11월 병행 운용이 이 5건에 걸려 있다. 고치는 것은
제출 직전 `getOrderbook` 한 번을 `PLANNED`에 싣는 것이고, 실패해도 주문은
그대로 나가야 한다(probe가 이미 그렇게 한다). **9월 동결 예외를 하나 더 둘지는
사장님이 정한다 — 오늘은 안 건드렸다.**

**② STATE 세 장이 비어 있다 — 9/14·9/15·9/16.** 판정기준 ③은 "아침 보고서를
못 읽은 날이 **연속 5거래일 이상**"이다. 9/17은 보고서를 읽은 기록이
`develope-log/2026-09-17_...`에 있고(9/16 보고서 숫자를 인용한다) 오늘이 이
장이므로 **연속은 3에서 끊겼다.** 다만 그 사흘의 마감 숫자는 복원되지 않는다.

**감성 표본은 오늘 움직였다.** 465건 · BLUESKY 235로 9/11과 건수는 같지만
점수가 −0.453이고 수집 시각도 0.254시간 전이다. 9/12에 "다음에도 235면 캐시를
의심한다"고 적었는데, 상한 셋(100·100·30)과 달리 235가 또 같은 것은
눈에 남긴다. **가중치 0이라 배분에는 영향이 없다.**

## 판독 뒤에 고친 것 둘 — 사장님 결정

**① ㉙ 주문 직전 호가를 기록한다.** 위 「걸린 것 ①」을 그대로 고쳤다. 표본 창이
3거래일뿐이라 9월 동결에 예외를 뒀다. `develope-log/2026-09-18_호가를-안-찍고-있었다.md`.

**② ㉚ 추세 일봉이 얼어붙으면 보고서가 말한다.** 점수 체계를 설명하다 나왔다 —
추세가 **죽으면** `⚠️ 비활성 신호`가 잡지만, 수집이 실패해도 캐시가 있으면
점수는 계속 나오고 `※ 캐시 사용` 넉 자뿐이었다. 캐시 나이와 수집 오류, 일부
실패한 종목을 적는다. **점수·비중·주문은 안 바뀐다.**
`develope-log/2026-09-18_추세가-얼어붙으면-알린다.md`.

**③ ㉛ 남은 잠금과 멈춘 사이클.** 사장님이 물어서 확인했다 — `paper-runner.lock`은
해제가 `finally`에만 달려 있어 Ctrl-C 한 번에 영구히 남을 수 있었다(재현함).
남으면 매매·대사가 멈추는데 보고서는 계속 나가서 **조용한 날과 구분되지 않는다.**
이제 오래된 잠금은 스스로 풀고, 보고서가 마지막 사이클 나이를 적는다.
`develope-log/2026-09-18_남은-잠금과-멈춘-사이클.md`.

## 안 한 것

**신호와 주문 경로는 안 건드렸다.** 위 둘은 재는 것과 말하는 것만 바꿨다. `DEVELOPE_IDEA/` 001·003·004는
10월 이후 그대로다. 대사 일수(9/2부터 11/60 예상)는 저널로 확인해야 하고,
그 명령은 `journalctl -u toss-ai-paper --since "2026-09-02" | grep -c "어긋납니다"`다.
