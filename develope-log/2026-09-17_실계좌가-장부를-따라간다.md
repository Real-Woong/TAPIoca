# 2026-09-17 · 실계좌가 장부를 따라간다

> 확정된 기록이다. 고치지 않는다. 현재 결론은 `../STRATEGY.md` ㉘에 있다.

## 사장님 말

> 내 목표는 "예수금 10만 원을 agent가 알아서 잘 갖고 놀아봐라, 수익 나게 로직
> 확인 겸"이었는데 지금 어떠한 이슈로 거의 실거래가 안 일어나고 있어.

9/16 보고서: 장부 $64.68 · 실계좌 $19.91 · 오늘의 실주문 없음. LIVE 12거래일째
실주문 0건이다.

## 원인 — ㉖에서 이미 적은 것

LIVE는 장부가 **새로 낸 결정**만 옮겼다. 장부는 07-14에 다 채웠고 그 뒤 결정은
밴드(5% ≈ $3.35)를 넘을 때만 나온다. **초기 매수를 내는 주체가 없었다.**
시장이 나빠서 쉰 것도, 현금이 없어서도 아니다(USD $69.87, ㉗).

9/12에 이것을 알았고 "돈이 나가는 일이라 9/30에 정한다"고 미뤘다. 사장님이
오늘 앞당겨 정했다. 물은 것은 둘이었다.

| 질문 | 답 |
|---|---|
| 어떻게 맞출까 | **매 사이클 자동으로** (한 번만 채우는 CLI · 9/30까지 대기 대신) |
| 맞춘 뒤 거래 빈도 | **전략 그대로** — 밴드를 좁히지 않는다 |

둘째 답이 중요하다. **맞춰도 주문은 드물다.** 연 회전율 2.8회짜리 전략이다.
이번 변경은 "거래를 많이 하게"가 아니라 "장부가 하는 것을 실계좌도 하게"다.

## 고친 것

- `src/live/ledger-sync.js` (새 파일) — 장부 수량과 실계좌 우리 몫의 차이를
  주문으로 바꾸는 순수 함수. 규칙은 STRATEGY ㉘ 표.
- `live-cycle.js` — `decide({ orders })`를 받는다. **1단계(미결 조회) 뒤의
  원장**으로 계산해야 해서 값이 아니라 함수다. 대사가 깨졌으면 안 부른다.
- `paper-runner.js` — 결정 대신 장부 보유·가격·한도를 넘긴다.
- `paper-bridge.js`와 그 테스트를 지웠다. 알림(RISK_ALERT 등)이 주문으로 새는
  문제는 이제 구조적으로 없다 — 동기화는 BUY·SELL만 만든다.
- 보고서 경고 문구: `장부가 새로 내는 주문만 실계좌로 나갑니다` →
  `실계좌가 매 사이클 한도 안에서 장부를 따라갑니다`.

테스트: `test/ledger-sync.test.js` 12개(9/16 계좌 재현 포함),
`test/live-cycle.test.js`에 `decide` 2개.

## 서버에서 볼 것

1. 배포 후 첫 정규장 사이클 로그의 `■ 실주문` — SCHD SELL $2.48이 먼저 나가야 한다.
2. **실계좌 금액 매도는 처음이다.** 거절되면 `거절:` 줄과 코드가 남는다.
3. 매일 아침 `⚠️ 장부 ≠ 실계좌` 차이가 **하루 $13 안팎씩 줄어드는지**. 4~5거래일
   뒤 경고 줄이 사라지면 끝이다. 안 줄면 `멈춤(...)` 줄을 먼저 본다.

## 병목을 쉽게 말하면

**에이전트가 "장부가 오늘 새로 하는 일"만 실계좌에서 따라 했는데, 할 일은
7월에 장부에서 이미 다 끝나 있었다.** 그래서 실계좌는 처음 사는 것부터 안 했다.
이제 규칙은 "장부가 들고 있는 만큼 실계좌도 들고 있어라"다.

**교훈: "새로 생긴 변화만 따라 하기"로 짜면 출발점이 다를 때 영원히 못 만난다.**
두 곳을 맞추려면 변화가 아니라 **상태**를 비교해야 한다. ㉖에서 원인을 보고도
"돈이 나가는 일"이라 미뤘고, 그 사이 5거래일이 더 0건이었다.

## 상한을 다시 확인했다 (사장님 질문)

> 전체 예수금 upper bound는 10만 원 맞지? 통장에 달러로 약 100만 원 있거든.

맞다. **동기화는 계좌 예수금을 한 번도 조회하지 않는다.** 막는 것은 셋이다 —
① 장부 원금 $67.05가 코드 상수(`ABSOLUTE_BUDGET_CAP_KRW`) ② 실계좌 우리 몫
평가액 ≤ 장부 자산(`ledger-sync.js`의 `maxLiveValueUsd`) ③ 1회 $6.7 · 하루 $13.4.
**정확한 상한은 10만 원이 아니라 `$67.05 + 손익`이다.** 매도는 우리가 체결시킨
수량 안에서만 나가고, 사장님이 관리 3종목을 손수 매매하면 대사가 잡아 멈춘다.

## 서버 반영

`git pull --ff-only && npm test` → **409/409 통과.** 서비스가 oneshot이라 재시작 없이
다음 사이클부터 새 코드다. **오늘 밤 첫 실주문 로그는 사장님이 붙여 주기로 했다 —
받으면 아래에 적는다.**

## 오늘 밤 결과

(대기)
