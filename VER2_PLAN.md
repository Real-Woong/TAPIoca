# ver2 — 대체자산 바스켓 (리서치 브랜치)

> **이 브랜치는 실제 돈을 만지지 않는다.** `main`이 실전이고 여기는 리서치다.
> 결론이 나면 `main`으로 가져간다. 결론이 안 나면 여기서 끝난다.
>
> 판정 기준은 **재기 전에** 적는다 — `../STRATEGY.md` §3·§6과 같은 규율이다.
> 마지막 갱신: 2026-08-21

---

## 왜 파는가

`main`의 ② 분해를 한계기여로 다시 읽으면 추세 층의 문제가 드러난다.

| | 실제 바스켓 2011~ | VTI,IWM 2011~ |
|---|---|---|
| 추세 **단독** 기여 (둘 다 끔 → 추세만) | +4.4%p | +5.5%p |
| 추세 **한계** 기여 (변동성만 → 현재 스택) | **+0.1%p** | +1.2%p |
| **중복분** | **4.3%p (98%)** | 4.3%p (78%) |

**추세는 약한 신호가 아니라 거의 완전히 중복된 신호다.** 혼자서는 4~5%p를
내는데, 변동성 관리를 켜면 그 값의 78~98%가 사라진다. 두 층이 **같은
손잡이(주식 노출 크기)**를 서로 다른 이유로 돌리기 때문이다. 추세가 팔고 싶은
순간에 변동성 관리가 이미 팔아 뒀다.

바스켓이 전부 미국 주식이라 **추세가 하방을 가리켜도 갈 곳이 현금뿐**이다.
대체자산은 추세에게 **없던 힘을 주는 것이 아니라 두 번째 축(자산 선택)을
주는 것**이다. 노출 크기는 변동성 관리가, 자산 선택은 추세가 맡으면 중복이
사라진다 — **그것이 이 브랜치의 가설이다.**

---

## 검증할 명제

> **확장 바스켓에서는 변동성 관리 없이 추세만으로도 유의미한 MDD 방어가 된다.**

---

## 1. 바스켓과 배분표

**리서치 바스켓: VTI · IWM · GLD · IEF · TLT + CASH** (2004~, 2008 포함)

| 앵커 | VTI | IWM | GLD | IEF | TLT | CASH | 주식계 |
|---|---|---|---|---|---|---|---|
| **RISK_ON** (+1.5) | 70 | 15 | 5 | 10 | 0 | 0 | 85 |
| **NEUTRAL** (0) | 55 | 5 | 10 | 20 | 0 | 10 | 60 |
| **RISK_OFF** (−1.5) | 25 | 0 | 15 | 25 | 10 | 25 | 25 |

- **SCHD를 뺐다.** 넣으면 표본이 2011~로 잘려 **2008을 잃는다**(main ⑮의 함정).
  SCHD의 역할("방어적 주식")은 진짜 방어자산이 들어오면 중복이다.
  2011~ 재현 검증 때만 다시 넣는다.
- **SHV를 뺐다.** 백테스터는 무위험이자율을 **0**으로 본다
  (`backtest-engine.js:216`). 지금 엔진에 SHV를 넣으면 그 우위의 상당 부분이
  **"현금은 0%"라는 가정의 그림자**다 — 자산이 아니라 엔진의 가정을 재게 된다.
  덤으로 공통 시작일이 2007(SHV) → **2004(GLD)**로 늘어난다.
- **TLT는 RISK_OFF에서만 10%.** 2022에 주식과 장기채가 함께 −20%대였다.
  듀레이션 헤지는 **금리 국면에 조건부**다. 주력은 IEF(7–10년).
- **GLD는 자체 변동성 15%대**라 안전자산이 아니다. 15% 상한.
- **RISK_OFF에도 현금 25%를 남긴다.** "모든 게 같이 떨어지는" 국면의 유일한
  대비다. 대체자산으로 전부 채우면 2022가 재현된다.

> ⚠️ **지금 구조로는 이 표에 도달할 수 없다.** `|score| ≤ 1`인데 경계가 1.5라
> 앵커까지 2/3만 간다(main ⑭). 실제 도달 가능한 최대 방어는 VTI 45 · GLD 13.3 ·
> IEF 23.3 · TLT 6.7 · 현금 20이다.
> **그래서 배분표와 경계는 함께 정해야 한다.** ⑭의 기각은 **현 바스켓에 대한
> 것**이므로 바스켓이 바뀌면 다시 열린 질문이다. 단 자동으로 열지 않고 §4의
> 실험 안에서 함께 잰다.

---

## 2. 먼저 고쳐야 할 두 곳 — 자산군 개념이 없다

| 곳 | 지금 동작 | 왜 깨지나 |
|---|---|---|
| `scaleForExposure()` | **CASH 아닌 모든 것**을 곱해서 깎는다 | 변동성이 오르면 **GLD·IEF까지 같이 팔고** 현금으로 간다. 헤지를 켜야 할 때 헤지를 판다 |
| `buildTrendSignal()`의 `volatility` | 워치리스트 **전 종목 연율변동성의 평균** | IEF(vol ~6%)가 섞이면 시장 변동성이 희석돼 **volTarget 0.15가 사실상 느슨해진다** |

```js
export const RISK_ASSETS = ["VTI", "SCHD", "IWM"];
export const DEFENSIVE   = ["GLD", "IEF", "TLT"];
```

변동성 **측정**과 노출 **스케일링**은 위험자산에만 적용한다.

---

## 3. Aggregator — 역상관 자산을 한 평균에 넣으면 안 된다

### 지금 공식을 그대로 쓰면 나는 사고

```
신뢰도 = coverage × |평균(sign)|
점수   = 평균(tanh) × 신뢰도 × 가중치
```

주식↓·채권↑ — **헤지가 작동하기 시작하는 바로 그 순간** sign이 갈려
`|평균(sign)|`이 무너지고 점수가 축소된다. **시스템이 방어해야 할 때 스스로를
무력화한다.** 튜닝 문제가 아니라 구조적 오류다.

### 역할 분리 — 한 점수를 두 점수로

```
레짐 점수   ← 위험자산 sleeve의 추세만.  "얼마나 위험을 질 것인가"
방어 배분   ← 방어자산 각각의 추세.      "물러설 때 어디로 갈 것인가"
```

방어자산의 추세는 레짐 점수에 **넣지 않는다.** TLT가 오르는 것은 "주식이
떨어질 것"의 증거가 아니라 "물러설 곳이 여기"의 증거다. 다른 질문에 답하는
신호를 한 평균에 넣으면 둘 다 망가진다.

바꿀 것 셋:

1. **가중 평균.** 지금 단순평균은 IWM(목표 0~15%)에게 VTI(70%)와 같은 표를 준다.
   NEUTRAL 목표 비중으로 가중한다.
2. **신뢰도는 위험자산 안에서만.** 위 사고를 막는다.
3. **방어 sleeve는 `score > 0`인 자산만 자격.** 아무도 없으면 전부 현금 —
   **2022 대비가 여기 들어간다.** "그나마 덜 나쁜 것"에 몰아주면 손실을 확정한다.

---

## 4. 백테스트 플랜 — 기각 조건은 재기 전에 적는다

### 노출 보정 기준선을 새로 세운다

**바스켓이 바뀌면 정적 사다리도 바뀐다.** 기존 VTI/현금 사다리를 쓰면
main ⑮에서 밟은 함정을 그대로 다시 밟는다. **같은 바스켓의 정적 배분 3점**으로
사다리를 세운다.

```
정적 A: VTI 60 · IEF 30 · GLD 10      정적 B: VTI 75 · IEF 15 · GLD 10
정적 C: VTI 90 · IEF  5 · GLD  5
```

### 2×2 요인 설계

| | volTarget 0.15 | volTarget 끔 |
|---|---|---|
| **현 바스켓** | 기준선 (= 지금 운영) | ② |
| **확장 바스켓** | ③ | **④ ← 명제의 핵심** |

- **성공**: ④의 노출보정 MDD 우위가 ②보다 **4/4로** 크고 기준선에 근접
  → 대체자산이 변동성 관리를 **대체한다**
- **부분 성공**: ③이 기준선을 4/4로 이김 → 대체는 못 해도 **보완**한다
- **기각**: ④가 ②와 구분되지 않음 → **추세의 무력함은 바스켓 문제가 아니었다**

### 기각 조건

- **노출 보정 후** 낙폭으로만 판정. 사다리는 **같은 바스켓**으로
- **부호일치 4/4.** 검정 수가 많아지므로(변형 ~8 × 지표 3 = 24, 우연한 4/4
  기대값 **3.0**) **4/4 하나를 채택 근거로 삼지 않는다**
- **회전율**은 08-06 손절 기각과 같은 자로 — +35~41%에 CDaR 1.0~1.6%p면 기각.
  스위칭은 회전율을 크게 올린다
- **틀 검증 필수**: "확장 바스켓 + 추세 끔 + 변동성 끔"이 **같은 바스켓의 정적
  배분과 같아야** 한다. 안 같으면 분석 틀이 깨진 것이니 숫자를 읽지 않는다
  (main ②가 세 표본에서 통과한 그 검사)

### 구간 분할로는 안 보이는 것 — 명시적 창

| 창 | 무엇을 보나 |
|---|---|
| 2008-09 ~ 2009-03 | 헤지가 **작동한** 국면 |
| **2022-01 ~ 2022-10** | 헤지가 **깨진** 국면. 현금 폴백이 실제로 발동하는가 |
| 2013-05 ~ 2013-09 | Taper tantrum. 주식·채권 동반 하락의 축소판 |

**2022가 이 설계의 시험대다.** 여기서 방어자산을 붙들고 있으면 배분표를 다시 짠다.

### 미리 인정하고 가는 편향

**GLD·TLT를 고른 것 자체가 사후 선택이다.** 2008에 무엇이 올랐는지 알고 고른다.
완화책은 하나뿐이다 — **바스켓과 배분표를 돌리기 전에 확정하고, 결과를 보고
비중을 튜닝하지 않는다.** 튜닝하면 그 순간 이 표본의 숫자는 근거가 아니다.

---

## 5. 10월에 쌓을 것은 전략이 아니라 체결 현실

**가격은 기다릴 필요가 없다.** TLT 2002·IEF 2002·GLD 2004부터 일봉이 전부
있으므로 `backtest:fetch` 한 번이면 20년치를 잰다. 감성 층과 결정적으로
다르다 — 그쪽은 수집창이 지나가면 복원이 안 돼서 로그를 쌓아야 했다.

10월까지 실제로 쌓아야 하는 것은 이것들이다.

| 질문 | 방법 |
|---|---|
| 토스에서 TLT/GLD/IEF가 실제로 매매되는가 | 시세 조회 + 소액 주문 1건 |
| 스프레드·체결 품질이 VTI와 다른가 | PAPER 병행 + 슬리피지 계측 |
| 소수점 매매가 되는가 | 실주문 1건 |

---

## 6. 두 인스턴스를 함께 돌릴 때의 규칙

`main`은 `~/toss-api/apps/toss-ai-agent`, ver2는 `~/TAPIOCA_ver2`로 분리한다.
**같은 토스 계정을 본다는 것이 위험의 전부다.**

| 규칙 | 왜 |
|---|---|
| **ver2는 절대 `LIVE_TRADING=true`가 될 수 없다** | 같은 계좌에 봇 둘이 주문하면 `live-position-baseline.json`이 폴더별로 따로라 **서로의 체결을 "남의 자산"으로 보고 대사가 깨진다.** 그러면 `main`이 영구 정지한다 |
| `data/`를 절대 공유하지 않는다 | 장부·원장·기준선이 섞인다 |
| ver2 타이머는 **30분 이상** | Toss API 한도는 계정 단위다. 15분 두 개면 429 위험 |
| ver2는 `.env`를 복사하되 **주문 관련 키는 비운다** | 실수의 경로를 물리적으로 없앤다 |

**`.env`에 맡기지 않고 코드로 막는다.** `main`에서 만든 `assertValidatedStack`
관문을 그대로 쓴다 — 미검증 바스켓은 미검증 층과 같은 취급이다.

---

## 7. main과의 관계

| | main | ver2 |
|---|---|---|
| 목적 | **실전** — 2026-09-01 전환, 현실 노이즈 실측 | 리서치 |
| 스택 | 거시 0 · 감성 0 · 추세 1 · MACD 0 · vol 0.15 · 경계 1.5 | 위 §1~3 |
| 실제 돈 | **쓴다 (10만 원)** | **절대 안 쓴다** |
| 판정 | 슬리피지·환전·괴리 (수익률 아님) | 위 §4 기각 조건 |

**서로 기다리지 않는다.** main의 9/1 전환은 ver2의 결론과 무관하고, ver2의
백테스트는 main의 운영과 무관하다.
