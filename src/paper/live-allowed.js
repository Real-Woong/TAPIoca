/**
 * **이 체크아웃이 실제 돈을 만질 수 있는가.**
 *
 * `main`은 `true`, 리서치 브랜치는 `false`다. **`.env`가 아니라 코드에 둔다** —
 * `.env`는 복사되고 사람은 실수한다. 이 파일이 두 브랜치의 유일한 차이여야
 * 병합할 때 충돌로 드러난다.
 *
 * ── 막으려는 사고 ────────────────────────────────────────────────────────
 *
 * `main`과 ver2는 **같은 토스 계정**을 본다. 둘 다 실주문을 내면:
 *
 *   1. `live-position-baseline.json`은 폴더마다 따로다
 *   2. 대사는 "브로커 보유 == 기준선 + 우리가 체결시킨 것"이다
 *   3. ver2가 산 것을 `main`은 **자기 기준선에 없는 남의 자산**으로 본다
 *   4. 대사가 깨진다 → `planCycle`이 멈춘다 → **`main`이 영구 정지한다**
 *
 * 실제 돈이 걸린 경로이고, 되돌리려면 기준선을 사람이 다시 세워야 한다.
 * 그래서 설정이 아니라 코드로 막는다.
 */
export const LIVE_TRADING_ALLOWED = false;

/** 오류 메시지에 어느 체크아웃인지 적기 위한 이름이다. */
export const CHECKOUT_NAME = "ver2 (리서치)";
